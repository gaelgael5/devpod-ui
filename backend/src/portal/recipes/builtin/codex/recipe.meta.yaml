id: codex
version: "1.0.0"
description: "OpenAI Codex CLI — auth via OPENAI_API_KEY au runtime"
options:
  version:
    type: string
    default: "latest"
    description: "Version npm (@openai/codex)"
requires_secrets:
  - path: "llm/openai_key"
    env: "OPENAI_API_KEY"
installs_after:
  - node
