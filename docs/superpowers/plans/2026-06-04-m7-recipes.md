# M7 — Registre de Recettes (devcontainer Features) — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permettre de composer des workspaces avec des « recettes » devcontainer Features (claude-code, aider, gemini-cli…), secrets résolus au runtime dans `remoteEnv`, jamais en build arg (§D-21).

**Architecture:**
- `portal/recipes/models.py` — `RecipeMeta` (pydantic v2) avec `SecretRef` (path + nom de variable env) pour un mapping explicite secret → env var ; `RecipeOption` pour les options de Feature.
- `portal/recipes/registry.py` — `RecipeRegistry` : chargement builtin / partagées / personnelles, tri topologique Kahn sur `installs_after`, détection de cycles.
- `portal/devpod/service.py` — `_write_devcontainer()` crée un tmpdir, y copie les Feature dirs, les référence en chemin local (`./id`) dans le devcontainer.json ; `up()` accepte `recipes: list[RecipeMeta]` + `feature_env: dict[str, str]`.
- `portal/routes/workspace_ops.py` — `UpRequest` gagne `recipes: list[str]` ; `workspace_up` résout l'ordre + les secrets avant d'appeler `svc.up()`.
- `portal/routes/recipes.py` — endpoints CRUD pour lister / gérer recettes partagées et personnelles.
- `portal/recipes/builtin/` — Features bundlées dans le package (claude-code + 6 autres).

**Tech Stack:** Python 3.12, pydantic v2, pyyaml, shutil (copytree/rmtree), collections.deque + defaultdict, pytest-asyncio

---

## Structure des fichiers

```
backend/src/portal/
  recipes/
    __init__.py               (nouveau — vide)
    models.py                 (nouveau — RecipeMeta, SecretRef, RecipeOption)
    registry.py               (nouveau — RecipeRegistry, CycleError, RecipeNotFoundError)
    builtin/
      claude-code/
        devcontainer-feature.json
        install.sh
        recipe.meta.yaml
      aider/           (idem)
      gemini-cli/      (idem)
      codex/           (idem)
      goose/           (idem)
      opencode/        (idem)
      cursor-agent/    (idem)
  devpod/
    service.py                (modifié — _write_devcontainer + up)
  routes/
    workspace_ops.py          (modifié — UpRequest + résolution recettes + secrets)
    recipes.py                (nouveau — endpoints)
  app.py                      (modifié — inclure recipes router)

backend/tests/
  recipes/
    __init__.py               (nouveau)
    test_models.py            (nouveau)
    test_registry.py          (nouveau)
    test_devcontainer_gen.py  (nouveau)
  routes/
    test_recipes.py           (nouveau)
```

---

## Contraintes critiques (lire avant tout)

- **§D-21** : les secrets vont dans `remoteEnv` du devcontainer.json. JAMAIS dans `features.options`, JAMAIS en build arg, JAMAIS dans les logs.
- **Portabilité** : le contenu des Feature dirs (install.sh, devcontainer-feature.json) ne contient aucun chemin `/data` ni référence au portail. Seul le devcontainer.json généré référence les chemins locaux — ce qui est attendu et non-portable, mais le _contenu_ des features est portable.
- **Path safety** : toute construction de chemin sous `/data` passe par `safe_user_path` ou `is_relative_to`.
- **Atomic writes** : `tempfile.mkstemp` + `os.replace` pour tout fichier de statut. `tempfile.mkdtemp` pour les répertoires temporaires devcontainer.

---

### Task 1 : Modèles pydantic `RecipeMeta`, `SecretRef`, `RecipeOption`

**Files:**
- Create: `backend/src/portal/recipes/__init__.py`
- Create: `backend/src/portal/recipes/models.py`
- Create: `backend/tests/recipes/__init__.py`
- Create: `backend/tests/recipes/test_models.py`

- [ ] **Step 1 : Écrire les tests qui échouent**

```python
# backend/tests/recipes/test_models.py
from __future__ import annotations

import yaml
import pytest


def test_recipe_meta_valid() -> None:
    from portal.recipes.models import RecipeMeta

    meta = RecipeMeta(
        id="claude-code",
        version="1.0.0",
        description="Claude Code CLI",
        requires_secrets=[{"path": "llm/anthropic_key", "env": "ANTHROPIC_API_KEY"}],
        installs_after=["node"],
    )
    assert meta.id == "claude-code"
    assert meta.requires_secrets[0].path == "llm/anthropic_key"
    assert meta.requires_secrets[0].env == "ANTHROPIC_API_KEY"


def test_recipe_meta_id_invalid() -> None:
    from pydantic import ValidationError
    from portal.recipes.models import RecipeMeta

    with pytest.raises(ValidationError, match="id"):
        RecipeMeta(id="INVALID NAME!")


def test_secret_ref_string_shorthand() -> None:
    from portal.recipes.models import RecipeMeta

    meta = RecipeMeta(id="aider", requires_secrets=["llm/anthropic_key"])
    assert meta.requires_secrets[0].path == "llm/anthropic_key"
    assert meta.requires_secrets[0].env == "LLM_ANTHROPIC_KEY"


def test_secret_ref_path_with_hyphen_normalised() -> None:
    from portal.recipes.models import RecipeMeta

    meta = RecipeMeta(id="aider", requires_secrets=["llm/gemini-key"])
    assert meta.requires_secrets[0].env == "LLM_GEMINI_KEY"


def test_secret_ref_invalid_path_rejected() -> None:
    from pydantic import ValidationError
    from portal.recipes.models import SecretRef

    with pytest.raises(ValidationError, match="secret path"):
        SecretRef(path="../traversal", env="TEST_VAR")


def test_secret_ref_invalid_env_rejected() -> None:
    from pydantic import ValidationError
    from portal.recipes.models import SecretRef

    with pytest.raises(ValidationError, match="env var"):
        SecretRef(path="my/secret", env="lowercase_not_allowed")


def test_recipe_option_defaults() -> None:
    from portal.recipes.models import RecipeOption

    opt = RecipeOption()
    assert opt.type == "string"
    assert opt.default == ""


def test_recipe_meta_from_yaml(tmp_path) -> None:
    from portal.recipes.models import RecipeMeta

    data = {
        "id": "claude-code",
        "version": "1.0.0",
        "description": "Claude Code CLI",
        "requires_secrets": [{"path": "llm/anthropic_key", "env": "ANTHROPIC_API_KEY"}],
        "installs_after": ["node"],
        "options": {"version": {"type": "string", "default": "latest"}},
    }
    meta_file = tmp_path / "recipe.meta.yaml"
    meta_file.write_text(yaml.dump(data), encoding="utf-8")

    meta = RecipeMeta.from_yaml(meta_file)
    assert meta.id == "claude-code"
    assert meta.options["version"].default == "latest"


def test_recipe_meta_extra_fields_rejected() -> None:
    from pydantic import ValidationError
    from portal.recipes.models import RecipeMeta

    with pytest.raises(ValidationError):
        RecipeMeta(id="x", unknown_field="bad")
```

- [ ] **Step 2 : Lancer les tests, vérifier qu'ils échouent**

```
cd backend && uv run pytest tests/recipes/test_models.py -v
```
Attendu : FAIL (ModuleNotFoundError ou ImportError)

- [ ] **Step 3 : Créer `backend/src/portal/recipes/__init__.py` (vide)**

```python
# backend/src/portal/recipes/__init__.py
```

- [ ] **Step 4 : Créer `backend/src/portal/recipes/models.py`**

```python
# backend/src/portal/recipes/models.py
from __future__ import annotations

import re
from pathlib import Path
from typing import Any

import yaml
from pydantic import BaseModel, ConfigDict, Field, field_validator

_RECIPE_ID_RE = re.compile(r"^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$")
_SECRET_PATH_RE = re.compile(r"^[a-zA-Z0-9][a-zA-Z0-9/_-]{0,127}$")
_ENV_VAR_RE = re.compile(r"^[A-Z][A-Z0-9_]{0,63}$")


class RecipeOption(BaseModel):
    model_config = ConfigDict(extra="forbid")

    type: str = "string"
    default: str = ""
    description: str = ""


class SecretRef(BaseModel):
    model_config = ConfigDict(extra="forbid")

    path: str
    env: str

    @field_validator("path")
    @classmethod
    def validate_path(cls, v: str) -> str:
        if not _SECRET_PATH_RE.fullmatch(v):
            raise ValueError(f"secret path {v!r} contient des caractères invalides")
        return v

    @field_validator("env")
    @classmethod
    def validate_env(cls, v: str) -> str:
        if not _ENV_VAR_RE.fullmatch(v):
            raise ValueError(f"env var name {v!r} must match ^[A-Z][A-Z0-9_]{{0,63}}$")
        return v


class RecipeMeta(BaseModel):
    model_config = ConfigDict(extra="forbid")

    id: str
    version: str = "1.0.0"
    description: str = ""
    options: dict[str, RecipeOption] = Field(default_factory=dict)
    requires_secrets: list[SecretRef] = Field(default_factory=list)
    installs_after: list[str] = Field(default_factory=list)

    @field_validator("id")
    @classmethod
    def validate_id(cls, v: str) -> str:
        if not _RECIPE_ID_RE.fullmatch(v):
            raise ValueError(
                f"id {v!r} must match ^[a-z0-9]([a-z0-9-]{{0,38}}[a-z0-9])?$"
            )
        return v

    @field_validator("requires_secrets", mode="before")
    @classmethod
    def normalize_secret_refs(cls, v: list[Any]) -> list[dict[str, str]]:
        """Accepte string courte ou dict explicite."""
        result = []
        for item in v:
            if isinstance(item, str):
                env = item.upper().replace("/", "_").replace("-", "_")
                result.append({"path": item, "env": env})
            else:
                result.append(item)
        return result

    @classmethod
    def from_yaml(cls, path: str | Path) -> "RecipeMeta":
        data: dict[str, Any] = yaml.safe_load(Path(path).read_text(encoding="utf-8"))
        return cls.model_validate(data)
```

- [ ] **Step 5 : Créer `backend/tests/recipes/__init__.py` (vide)**

- [ ] **Step 6 : Lancer les tests, vérifier qu'ils passent**

```
cd backend && uv run pytest tests/recipes/test_models.py -v
```
Attendu : 9 tests PASSED

- [ ] **Step 7 : Lint + mypy**

```
cd backend && uv run ruff check src/portal/recipes/models.py tests/recipes/test_models.py
cd backend && uv run mypy src/portal/recipes/models.py
```

- [ ] **Step 8 : Commit**

```
git add backend/src/portal/recipes/__init__.py backend/src/portal/recipes/models.py \
        backend/tests/recipes/__init__.py backend/tests/recipes/test_models.py
git commit -m "feat(recipes): ajouter modèles pydantic RecipeMeta + SecretRef"
```

---

### Task 2 : `RecipeRegistry` — chargement et tri topologique

**Files:**
- Create: `backend/src/portal/recipes/registry.py`
- Create: `backend/tests/recipes/test_registry.py`

- [ ] **Step 1 : Écrire les tests qui échouent**

```python
# backend/tests/recipes/test_registry.py
from __future__ import annotations

from pathlib import Path

import pytest
import yaml


def _write_recipe(
    base: Path,
    recipe_id: str,
    installs_after: list[str] | None = None,
    requires_secrets: list | None = None,
) -> None:
    d = base / recipe_id
    d.mkdir(parents=True, exist_ok=True)
    meta = {
        "id": recipe_id,
        "version": "1.0.0",
        "description": f"Recipe {recipe_id}",
        "installs_after": installs_after or [],
        "requires_secrets": requires_secrets or [],
    }
    (d / "recipe.meta.yaml").write_text(yaml.dump(meta), encoding="utf-8")
    (d / "devcontainer-feature.json").write_text(
        f'{{"id": "{recipe_id}", "version": "1.0.0"}}', encoding="utf-8"
    )
    (d / "install.sh").write_text("#!/usr/bin/env bash\necho installed\n", encoding="utf-8")


def test_load_dir_nonexistent_returns_empty(tmp_path: Path) -> None:
    from portal.recipes.registry import RecipeRegistry

    registry = RecipeRegistry()
    assert registry.load_dir(tmp_path / "nonexistent") == {}


def test_load_dir_loads_recipes(tmp_path: Path) -> None:
    from portal.recipes.registry import RecipeRegistry

    _write_recipe(tmp_path, "claude-code")
    _write_recipe(tmp_path, "aider")
    registry = RecipeRegistry()
    result = registry.load_dir(tmp_path)
    assert set(result.keys()) == {"claude-code", "aider"}


def test_load_dir_ignores_dir_without_meta(tmp_path: Path) -> None:
    from portal.recipes.registry import RecipeRegistry

    (tmp_path / "not-a-recipe").mkdir()
    registry = RecipeRegistry()
    result = registry.load_dir(tmp_path)
    assert result == {}


def test_load_shared_builtin_overridden_by_shared(tmp_path: Path) -> None:
    builtin = tmp_path / "builtin"
    shared = tmp_path / "shared"
    _write_recipe(builtin, "claude-code")
    _write_recipe(shared, "claude-code")
    from portal.recipes.registry import RecipeRegistry

    registry = RecipeRegistry(builtin_dir=builtin, shared_dir=shared)
    result = registry.load_shared()
    assert len(result) == 1
    assert "claude-code" in result


def test_resolve_order_respects_installs_after(tmp_path: Path) -> None:
    from portal.recipes.registry import RecipeRegistry

    _write_recipe(tmp_path, "base")
    _write_recipe(tmp_path, "node", installs_after=["base"])
    _write_recipe(tmp_path, "claude-code", installs_after=["node"])
    registry = RecipeRegistry()
    available = registry.load_dir(tmp_path)
    order = registry.resolve_order(["claude-code", "node", "base"], available)
    ids = [r.id for r in order]
    assert ids.index("base") < ids.index("node") < ids.index("claude-code")


def test_resolve_order_unknown_recipe_raises(tmp_path: Path) -> None:
    from portal.recipes.registry import RecipeRegistry, RecipeNotFoundError

    registry = RecipeRegistry()
    with pytest.raises(RecipeNotFoundError, match="unknown-recipe"):
        registry.resolve_order(["unknown-recipe"], {})


def test_resolve_order_cycle_raises(tmp_path: Path) -> None:
    from portal.recipes.registry import CycleError, RecipeRegistry

    _write_recipe(tmp_path, "a", installs_after=["b"])
    _write_recipe(tmp_path, "b", installs_after=["a"])
    registry = RecipeRegistry()
    available = registry.load_dir(tmp_path)
    with pytest.raises(CycleError):
        registry.resolve_order(["a", "b"], available)


def test_resolve_order_no_deps_preserves_selection(tmp_path: Path) -> None:
    from portal.recipes.registry import RecipeRegistry

    _write_recipe(tmp_path, "a")
    _write_recipe(tmp_path, "b")
    registry = RecipeRegistry()
    available = registry.load_dir(tmp_path)
    order = registry.resolve_order(["a", "b"], available)
    assert len(order) == 2


def test_personal_overrides_shared(tmp_path: Path) -> None:
    shared = tmp_path / "shared"
    personal = tmp_path / "personal"
    _write_recipe(shared, "my-recipe")
    _write_recipe(personal, "my-recipe")
    from portal.recipes.registry import RecipeRegistry

    registry = RecipeRegistry(shared_dir=shared)
    shared_recipes = registry.load_shared()
    personal_recipes = registry.load_dir(personal)
    available = {**shared_recipes, **personal_recipes}
    assert "my-recipe" in available
    assert len(available) == 1
```

- [ ] **Step 2 : Lancer les tests, vérifier qu'ils échouent**

```
cd backend && uv run pytest tests/recipes/test_registry.py -v
```
Attendu : FAIL (ModuleNotFoundError)

- [ ] **Step 3 : Créer `backend/src/portal/recipes/registry.py`**

```python
# backend/src/portal/recipes/registry.py
from __future__ import annotations

from collections import defaultdict, deque
from pathlib import Path

import yaml

from .models import RecipeMeta

BUILTIN_DIR: Path = Path(__file__).parent / "builtin"


class CycleError(ValueError):
    """Dépendance circulaire détectée dans installs_after."""


class RecipeNotFoundError(KeyError):
    """Recette demandée introuvable dans le registre disponible."""


class RecipeRegistry:
    def __init__(
        self,
        builtin_dir: Path | None = None,
        shared_dir: Path | None = None,
    ) -> None:
        self._builtin_dir: Path | None = builtin_dir if builtin_dir is not None else BUILTIN_DIR
        self._shared_dir: Path | None = shared_dir

    def load_shared(self) -> dict[str, RecipeMeta]:
        """Charge builtin puis partagées admin (partagées écrasent builtin à id égal)."""
        recipes: dict[str, RecipeMeta] = {}
        for directory in (self._builtin_dir, self._shared_dir):
            if directory is not None:
                recipes.update(self.load_dir(directory))
        return recipes

    def load_dir(self, directory: Path) -> dict[str, RecipeMeta]:
        """Charge toutes les recettes valides depuis un répertoire (non-récursif)."""
        recipes: dict[str, RecipeMeta] = {}
        if not directory.exists():
            return recipes
        for entry in sorted(directory.iterdir()):
            if entry.is_dir():
                meta = self._load_meta(entry)
                if meta is not None:
                    recipes[meta.id] = meta
        return recipes

    def resolve_order(
        self,
        selected_ids: list[str],
        available: dict[str, RecipeMeta],
    ) -> list[RecipeMeta]:
        """Tri topologique Kahn sur installs_after.

        Lève RecipeNotFoundError si un id est inconnu.
        Lève CycleError si un cycle est détecté.
        """
        for rid in selected_ids:
            if rid not in available:
                raise RecipeNotFoundError(f"Recipe {rid!r} introuvable dans le registre")

        selected_set = set(selected_ids)
        in_degree: dict[str, int] = {rid: 0 for rid in selected_ids}
        dependents: defaultdict[str, list[str]] = defaultdict(list)

        for rid in selected_ids:
            for dep in available[rid].installs_after:
                if dep in selected_set:
                    in_degree[rid] += 1
                    dependents[dep].append(rid)

        queue: deque[str] = deque(
            rid for rid in selected_ids if in_degree[rid] == 0
        )
        result: list[RecipeMeta] = []

        while queue:
            rid = queue.popleft()
            result.append(available[rid])
            for dependent in sorted(dependents[rid]):
                in_degree[dependent] -= 1
                if in_degree[dependent] == 0:
                    queue.append(dependent)

        if len(result) != len(selected_ids):
            raise CycleError(
                f"Cycle détecté dans installs_after parmi : {selected_ids}"
            )

        return result

    @staticmethod
    def _load_meta(recipe_dir: Path) -> RecipeMeta | None:
        meta_file = recipe_dir / "recipe.meta.yaml"
        if not meta_file.exists():
            return None
        try:
            data: object = yaml.safe_load(meta_file.read_text(encoding="utf-8"))
            return RecipeMeta.model_validate(data)
        except Exception:
            return None
```

- [ ] **Step 4 : Lancer les tests, vérifier qu'ils passent**

```
cd backend && uv run pytest tests/recipes/test_registry.py -v
```
Attendu : 9 tests PASSED

- [ ] **Step 5 : Lint + mypy**

```
cd backend && uv run ruff check src/portal/recipes/registry.py tests/recipes/test_registry.py
cd backend && uv run mypy src/portal/recipes/registry.py
```

- [ ] **Step 6 : Commit**

```
git add backend/src/portal/recipes/registry.py backend/tests/recipes/test_registry.py
git commit -m "feat(recipes): ajouter RecipeRegistry avec tri topologique Kahn"
```

---

### Task 3 : Génération devcontainer avec recettes + résolution secrets

**Files:**
- Modify: `backend/src/portal/devpod/service.py`
- Modify: `backend/src/portal/routes/workspace_ops.py`
- Create: `backend/tests/recipes/test_devcontainer_gen.py`

**Contexte :**
- `_write_devcontainer()` passe de `tempfile.mkstemp` à `tempfile.mkdtemp` (répertoire), y copie les Feature dirs avec `shutil.copytree`, les référence par chemin local (`"./{id}"`) dans le devcontainer.json.
- `_run_up_task()` cleanup passe de `dc_path.unlink()` à `shutil.rmtree(dc_path.parent, ...)`.
- `up()` gagne deux paramètres optionnels : `recipes: list[RecipeMeta] | None = None` et `feature_env: dict[str, str] | None = None`.
- `UpRequest` gagne `recipes: list[str] = []`.
- `workspace_up` résout recettes + secrets via `asyncio.to_thread`.
- §D-21 : secrets → `remoteEnv` uniquement, JAMAIS dans `features.{id}` options.

- [ ] **Step 1 : Écrire les tests qui échouent**

```python
# backend/tests/recipes/test_devcontainer_gen.py
from __future__ import annotations

import json
import shutil
from pathlib import Path

import pytest
import yaml


def _make_builtin(tmp_path: Path, recipe_id: str, requires_secrets: list | None = None) -> Path:
    """Crée une Feature dir builtin minimale pour les tests."""
    d = tmp_path / recipe_id
    d.mkdir(parents=True)
    (d / "devcontainer-feature.json").write_text(
        json.dumps({"id": recipe_id, "version": "1.0.0"}), encoding="utf-8"
    )
    (d / "install.sh").write_text("#!/usr/bin/env bash\necho ok\n", encoding="utf-8")
    (d / "recipe.meta.yaml").write_text(
        yaml.dump(
            {
                "id": recipe_id,
                "version": "1.0.0",
                "requires_secrets": requires_secrets or [],
            }
        ),
        encoding="utf-8",
    )
    return d.parent  # retourne le répertoire builtin


@pytest.fixture
def global_cfg(tmp_data_root):
    from portal.config.store import load_global

    return load_global()


@pytest.fixture
def fake_devpod_bin(tmp_path):
    from tests.devpod.fake_devpod import FAKE_DEVPOD_BIN

    return FAKE_DEVPOD_BIN


def test_write_devcontainer_no_recipes(tmp_data_root, global_cfg, fake_devpod_bin) -> None:
    """Sans recettes, devcontainer.json contient image de base et pas de features."""
    from portal.auth.router import provision_user
    from portal.devpod.service import DevPodService

    import asyncio

    asyncio.get_event_loop().run_until_complete(
        provision_user(login="alice", sub="sub", data_root=tmp_data_root)
    )
    svc = DevPodService(global_cfg=global_cfg, devpod_bin=fake_devpod_bin)
    dc_path = svc._write_devcontainer("alice", "alice-myapp")
    content = json.loads(dc_path.read_text(encoding="utf-8"))
    assert "image" in content
    assert "features" not in content
    assert "remoteEnv" not in content
    # Nettoyage
    shutil.rmtree(dc_path.parent, ignore_errors=True)


def test_write_devcontainer_with_recipe_copies_feature_dir(tmp_data_root, tmp_path, global_cfg, fake_devpod_bin) -> None:
    """Avec une recette, la Feature dir est copiée et référencée en chemin local."""
    from portal.auth.router import provision_user
    from portal.devpod.service import DevPodService
    from portal.recipes.models import RecipeMeta

    import asyncio

    asyncio.get_event_loop().run_until_complete(
        provision_user(login="alice", sub="sub", data_root=tmp_data_root)
    )
    builtin_dir = tmp_path / "builtin"
    _make_builtin(builtin_dir, "claude-code")
    recipe = RecipeMeta(id="claude-code")

    svc = DevPodService(
        global_cfg=global_cfg,
        devpod_bin=fake_devpod_bin,
        recipes_builtin_dir=builtin_dir,
    )
    dc_path = svc._write_devcontainer("alice", "alice-myapp", recipes=[recipe])
    content = json.loads(dc_path.read_text(encoding="utf-8"))
    assert "features" in content
    assert "./claude-code" in content["features"]
    # Feature dir copiée dans le tmpdir
    assert (dc_path.parent / "claude-code" / "install.sh").exists()
    shutil.rmtree(dc_path.parent, ignore_errors=True)


def test_write_devcontainer_secrets_in_remote_env_not_features(tmp_data_root, tmp_path, global_cfg, fake_devpod_bin) -> None:
    """§D-21 : les secrets vont dans remoteEnv, PAS dans les options features."""
    from portal.auth.router import provision_user
    from portal.devpod.service import DevPodService
    from portal.recipes.models import RecipeMeta

    import asyncio

    asyncio.get_event_loop().run_until_complete(
        provision_user(login="alice", sub="sub", data_root=tmp_data_root)
    )
    builtin_dir = tmp_path / "builtin"
    _make_builtin(builtin_dir, "aider")
    recipe = RecipeMeta(id="aider")
    feature_env = {"ANTHROPIC_API_KEY": "sk-secret-value"}

    svc = DevPodService(
        global_cfg=global_cfg,
        devpod_bin=fake_devpod_bin,
        recipes_builtin_dir=builtin_dir,
    )
    dc_path = svc._write_devcontainer(
        "alice", "alice-myapp", recipes=[recipe], feature_env=feature_env
    )
    content = json.loads(dc_path.read_text(encoding="utf-8"))
    # Secret dans remoteEnv
    assert content["remoteEnv"]["ANTHROPIC_API_KEY"] == "sk-secret-value"
    # Secret ABSENT des options de features
    features_str = json.dumps(content.get("features", {}))
    assert "sk-secret-value" not in features_str
    shutil.rmtree(dc_path.parent, ignore_errors=True)


def test_write_devcontainer_cleanup_on_error(tmp_data_root, tmp_path, global_cfg, fake_devpod_bin) -> None:
    """Si _write_devcontainer échoue, le tmpdir est nettoyé (pas de fuite disque)."""
    from portal.auth.router import provision_user
    from portal.devpod.service import DevPodService
    from portal.recipes.models import RecipeMeta

    import asyncio

    asyncio.get_event_loop().run_until_complete(
        provision_user(login="alice", sub="sub", data_root=tmp_data_root)
    )
    # Feature dir inexistante → pas d'erreur (ignorée silencieusement), le tmpdir reste propre
    recipe = RecipeMeta(id="nonexistent")
    svc = DevPodService(
        global_cfg=global_cfg,
        devpod_bin=fake_devpod_bin,
        recipes_builtin_dir=tmp_path / "empty-builtin",
    )
    dc_path = svc._write_devcontainer("alice", "alice-myapp", recipes=[recipe])
    content = json.loads(dc_path.read_text(encoding="utf-8"))
    # La feature manquante est ignorée — pas de crash, pas de features block
    assert "features" not in content
    shutil.rmtree(dc_path.parent, ignore_errors=True)
```

- [ ] **Step 2 : Lancer les tests, vérifier qu'ils échouent**

```
cd backend && uv run pytest tests/recipes/test_devcontainer_gen.py -v
```
Attendu : FAIL

- [ ] **Step 3 : Modifier `backend/src/portal/devpod/service.py`**

Voici le fichier complet après modifications :

```python
# backend/src/portal/devpod/service.py
from __future__ import annotations

import asyncio
import contextlib
import json
import os
import re
import shutil
import tempfile
from pathlib import Path
from typing import TYPE_CHECKING, Any
from urllib.parse import urlparse

import structlog

from ..config.models import GlobalConfig, WorkspaceSpec
from ..config.store import _data_root, safe_user_path
from ..recipes.models import RecipeMeta
from .env import build_env
from .provider import ensure_provider
from .runner import run_subprocess

if TYPE_CHECKING:
    from ..exposure import ExposureService

_log = structlog.get_logger(__name__)

_WS_ID_RE = re.compile(r"^[a-z0-9][a-z0-9-]{0,61}[a-z0-9]$")

# Répertoire des Features builtin (relatif au package)
_RECIPES_BUILTIN_DIR = Path(__file__).parent.parent / "recipes" / "builtin"


class DevPodService:
    def __init__(
        self,
        global_cfg: GlobalConfig,
        devpod_bin: list[str] | None = None,
        exposure: ExposureService | None = None,
        recipes_builtin_dir: Path | None = None,
    ) -> None:
        self._global_cfg = global_cfg
        self._devpod_bin: list[str] = (
            devpod_bin if devpod_bin is not None else [global_cfg.devpod.binary]
        )
        self._exposure = exposure
        self._background_tasks: set[asyncio.Task[None]] = set()
        self._recipes_builtin_dir: Path = (
            recipes_builtin_dir if recipes_builtin_dir is not None else _RECIPES_BUILTIN_DIR
        )

    # ------------------------------------------------------------------
    # Public interface
    # ------------------------------------------------------------------

    def _ws_id(self, login: str, name: str) -> str:
        ws_id = f"{login}-{name}"
        if not _WS_ID_RE.fullmatch(ws_id):
            raise ValueError(f"Computed ws_id {ws_id!r} is not DNS-safe")
        return ws_id

    async def up(
        self,
        login: str,
        ws_spec: WorkspaceSpec,
        recipes: list[RecipeMeta] | None = None,
        feature_env: dict[str, str] | None = None,
    ) -> str:
        """Lance un workspace en tâche de fond. Retourne ws_id immédiatement."""
        ws_id = self._ws_id(login, ws_spec.name)

        base_env = build_env(login=login, ws_spec=ws_spec, global_cfg=self._global_cfg)

        host_type = self._resolve_host_type(ws_spec)
        await ensure_provider(
            login=login,
            host_type=host_type,
            env=base_env,
            devpod_bin=self._devpod_bin,
        )

        host_port: int | None = None
        if self._exposure is not None:
            host_port = await self._exposure.allocate_port(ws_id)

        dc_path = self._write_devcontainer(
            login,
            ws_id,
            host_port=host_port,
            recipes=recipes,
            feature_env=feature_env,
        )

        subprocess_env = {**base_env, **ws_spec.env}
        node_ip = self._resolve_node_ip(ws_spec.host)
        self._write_status(ws_id, "provisioning", login=login)

        task = asyncio.create_task(
            self._run_up_task(
                ws_id, ws_spec.source, dc_path, subprocess_env, login, host_port, node_ip
            )
        )
        self._background_tasks.add(task)
        task.add_done_callback(self._background_tasks.discard)
        _log.info("workspace_up_started", ws_id=ws_id, login=login)
        return ws_id

    async def stop(self, login: str, ws_id: str) -> None:
        if self._exposure is not None:
            await self._exposure.unexpose(ws_id)
        env = self._minimal_env(login)
        cmd = [*self._devpod_bin, "stop", ws_id]
        log_path = self._log_path(login, f"{ws_id}-stop")
        await run_subprocess(cmd=cmd, env=env, log_path=log_path, ws_id=ws_id)
        self._write_status(ws_id, "stopped", login=login)
        _log.info("workspace_stopped", ws_id=ws_id, login=login)

    async def delete(self, login: str, ws_id: str) -> None:
        if self._exposure is not None:
            await self._exposure.unexpose(ws_id)
        env = self._minimal_env(login)
        cmd = [*self._devpod_bin, "delete", ws_id, "--force"]
        log_path = self._log_path(login, f"{ws_id}-delete")
        await run_subprocess(cmd=cmd, env=env, log_path=log_path, ws_id=ws_id)
        status_path = self._status_path(ws_id)
        if status_path.exists():
            status_path.unlink()
        _log.info("workspace_deleted", ws_id=ws_id, login=login)

    async def status(self, login: str, ws_id: str) -> dict[str, Any]:
        path = self._status_path(ws_id)
        if not path.exists():
            return {"ws_id": ws_id, "status": "unknown"}
        return json.loads(path.read_text(encoding="utf-8"))  # type: ignore[no-any-return]

    async def list_workspaces(self, login: str) -> list[dict[str, Any]]:
        routes_dir = _data_root() / "routes"
        if not routes_dir.exists():
            return []
        results: list[dict[str, Any]] = []
        for f in routes_dir.glob("*.json"):
            try:
                data = json.loads(f.read_text(encoding="utf-8"))
                if data.get("login") == login:
                    results.append(data)
            except json.JSONDecodeError:
                pass
        return results

    def get_port(self, ws_id: str) -> int | None:
        path = self._status_path(ws_id)
        if not path.exists():
            return None
        try:
            data = json.loads(path.read_text(encoding="utf-8"))
            p = data.get("host_port")
            return int(p) if p is not None else None
        except (json.JSONDecodeError, ValueError, OSError):
            return None

    # ------------------------------------------------------------------
    # Internal helpers
    # ------------------------------------------------------------------

    def _status_path(self, ws_id: str) -> Path:
        return _data_root() / "routes" / f"{ws_id}.json"

    def _log_path(self, login: str, ws_id: str) -> Path:
        return _data_root() / "logs" / login / f"{ws_id}.log"

    def _write_status(self, ws_id: str, status: str, login: str = "", **extra: Any) -> None:
        path = self._status_path(ws_id)
        path.parent.mkdir(parents=True, exist_ok=True)
        data: dict[str, Any] = {"ws_id": ws_id, "status": status}
        if login:
            data["login"] = login
        data.update(extra)
        fd, tmp_path = tempfile.mkstemp(dir=path.parent, suffix=f"-{ws_id}.tmp")
        try:
            with os.fdopen(fd, "w", encoding="utf-8") as f:
                json.dump(data, f)
            os.replace(tmp_path, path)
        except Exception:
            with contextlib.suppress(OSError):
                os.unlink(tmp_path)
            raise

    def _write_devcontainer(
        self,
        login: str,
        ws_id: str,
        host_port: int | None = None,
        recipes: list[RecipeMeta] | None = None,
        feature_env: dict[str, str] | None = None,
    ) -> Path:
        """Écrit devcontainer.json + Feature dirs dans un tmpdir. Retourne le chemin du JSON."""
        user_dir = safe_user_path(login, "devpod")
        user_dir.mkdir(parents=True, exist_ok=True)

        tmp_dir = Path(tempfile.mkdtemp(dir=user_dir, prefix=f"{ws_id}-dc-"))
        try:
            content: dict[str, Any] = {
                "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
            }
            if host_port is not None:
                content["appPorts"] = [f"{host_port}:3000"]

            if recipes:
                features_block: dict[str, dict[str, Any]] = {}
                for recipe in recipes:
                    src = self._recipes_builtin_dir / recipe.id
                    if src.is_dir():
                        shutil.copytree(src, tmp_dir / recipe.id)
                        features_block[f"./{recipe.id}"] = {}
                if features_block:
                    content["features"] = features_block

            if feature_env:
                content["remoteEnv"] = dict(feature_env)

            dc_path = tmp_dir / "devcontainer.json"
            dc_path.write_text(json.dumps(content, indent=2), encoding="utf-8")
            return dc_path
        except Exception:
            shutil.rmtree(tmp_dir, ignore_errors=True)
            raise

    def _resolve_host_type(self, ws_spec: WorkspaceSpec) -> str:
        host_name = ws_spec.host
        if not host_name:
            defaults = [h for h in self._global_cfg.hosts if h.default]
            return defaults[0].type if defaults else "docker-tls"
        for h in self._global_cfg.hosts:
            if h.name == host_name:
                return h.type
        return "docker-tls"

    def _resolve_node_ip(self, host_name: str) -> str:
        if not host_name:
            defaults = [h for h in self._global_cfg.hosts if h.default]
            host = defaults[0] if defaults else None
        else:
            host = next((h for h in self._global_cfg.hosts if h.name == host_name), None)
        if host and host.docker_host:
            return urlparse(host.docker_host).hostname or "127.0.0.1"
        return "127.0.0.1"

    def _minimal_env(self, login: str) -> dict[str, str]:
        return {
            "PATH": os.environ.get("PATH", ""),
            "DEVPOD_HOME": str(safe_user_path(login, "devpod")),
        }

    async def _run_up_task(
        self,
        ws_id: str,
        source: str,
        dc_path: Path,
        env: dict[str, str],
        login: str,
        host_port: int | None = None,
        node_ip: str = "127.0.0.1",
    ) -> None:
        try:
            cmd = [
                *self._devpod_bin,
                "up",
                "--id",
                ws_id,
                "--ide",
                "openvscode",
                "--devcontainer-path",
                str(dc_path),
                "--open-ide=false",
                "--",
                source,
            ]
            log_path = self._log_path(login, ws_id)
            returncode = await run_subprocess(cmd=cmd, env=env, log_path=log_path, ws_id=ws_id)
            status = "running" if returncode == 0 else "failed"
            extra: dict[str, Any] = {"returncode": returncode}
            if status == "running" and self._exposure is not None and host_port is not None:
                try:
                    url = await self._exposure.expose(
                        ws_id=ws_id,
                        node_ip=node_ip,
                        host_port=host_port,
                    )
                    extra["url"] = url
                    extra["host_port"] = host_port
                except Exception as exc:
                    _log.error(
                        "workspace_expose_failed",
                        ws_id=ws_id,
                        error=type(exc).__name__,
                    )
            elif host_port is not None:
                extra["host_port"] = host_port
            self._write_status(ws_id, status, login=login, **extra)
            if returncode != 0:
                _log.warning("workspace_up_failed", ws_id=ws_id, returncode=returncode)
            else:
                _log.info("workspace_up_done", ws_id=ws_id, login=login)
        except Exception as exc:
            self._write_status(ws_id, "failed", login=login, error=type(exc).__name__)
            _log.error("workspace_up_crashed", ws_id=ws_id, error=type(exc).__name__)
        finally:
            # Nettoyage du répertoire temporaire (devcontainer.json + Feature dirs copiées)
            with contextlib.suppress(Exception):
                shutil.rmtree(dc_path.parent, ignore_errors=True)
```

- [ ] **Step 4 : Modifier `backend/src/portal/routes/workspace_ops.py`**

Voici les modifications à apporter (ajouter `recipes` à `UpRequest`, ajouter `_get_recipe_registry`, modifier `workspace_up`) :

```python
# backend/src/portal/routes/workspace_ops.py
from __future__ import annotations

import asyncio
import os
import re
import shlex
from typing import Any

import structlog
from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel, ConfigDict, Field

from ..auth.rbac import UserInfo, require_user
from ..config.store import load_global, safe_user_path
from ..devpod.env import UnknownHostError
from ..devpod.service import DevPodService
from ..recipes.models import RecipeMeta, SecretRef
from ..recipes.registry import RecipeRegistry

_log = structlog.get_logger(__name__)
router = APIRouter(tags=["workspace-ops"])

_NAME_RE = re.compile(r"^[a-z0-9][a-z0-9-]{0,30}[a-z0-9]$")
_RECIPE_ID_RE = re.compile(r"^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$")


def _validate_name(name: str) -> None:
    if not _NAME_RE.fullmatch(name):
        raise HTTPException(
            status_code=422,
            detail=(
                f"Invalid workspace name {name!r}: must match ^[a-z0-9][a-z0-9-]{{0,30}}[a-z0-9]$"
            ),
        )


class UpRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    source: str
    host: str = ""
    recipes: list[str] = Field(default_factory=list)


_service: DevPodService | None = None
_recipe_registry: RecipeRegistry | None = None


def _get_service() -> DevPodService:
    global _service
    if _service is None:
        _service = _build_service()
    return _service


def _build_service() -> DevPodService:
    import httpx

    from ..config.store import _data_root
    from ..exposure import ExposureService
    from ..exposure.caddy import CaddyClient
    from ..exposure.ports import PortRegistry

    global_cfg = load_global()
    devpod_bin = shlex.split(global_cfg.devpod.binary, posix=(os.name != "nt"))

    exposure: ExposureService | None = None
    if global_cfg.caddy.admin_api:
        data_root = _data_root()
        verify_uri = f"{global_cfg.server.external_url}/auth/caddy/verify"
        caddy = CaddyClient(
            admin_api=global_cfg.caddy.admin_api,
            http_client=httpx.AsyncClient(),
            verify_uri=verify_uri,
        )
        registry = PortRegistry(data_root)
        exposure = ExposureService(
            caddy=caddy,
            registry=registry,
            data_root=data_root,
            base_domain=global_cfg.server.base_domain,
        )

    return DevPodService(global_cfg=global_cfg, devpod_bin=devpod_bin, exposure=exposure)


def _get_recipe_registry() -> RecipeRegistry:
    global _recipe_registry
    if _recipe_registry is None:
        from ..config.store import _data_root

        _recipe_registry = RecipeRegistry(shared_dir=_data_root() / "recipes")
    return _recipe_registry


def _reset_service() -> None:
    global _service, _recipe_registry
    _service = None
    _recipe_registry = None


def _resolve_feature_secrets(login: str, secret_refs: list[SecretRef]) -> dict[str, str]:
    """Résout les secrets de recettes dans le thread I/O (pas dans le thread async)."""
    from ..config.store import _data_root, load_user
    from ..secrets.factory import create_backend
    from ..secrets.resolver import Scope, resolve
    from ..secrets.types import Secret

    user_cfg = load_user(login)
    global_cfg = load_global()
    user_secrets_path = safe_user_path(login, "secrets.yaml")
    backend = create_backend(
        backend_type=global_cfg.secrets.backend,
        url=global_cfg.secrets.harpocrate.url,
        api_key=global_cfg.secrets.harpocrate.api_key,
        base_path=global_cfg.secrets.harpocrate.base_path,
        user_secrets_path=user_secrets_path,
    )
    scope = Scope(kind="user", secret_ns=user_cfg.secret_ns, login=login)
    result: dict[str, str] = {}
    for ref in secret_refs:
        val = resolve(ref.path, scope, backend)
        result[ref.env] = val.reveal() if isinstance(val, Secret) else str(val)
    return result


@router.post("/workspaces/{name}/up", status_code=202)
async def workspace_up(
    name: str,
    req: UpRequest,
    user: UserInfo = Depends(require_user),
) -> dict[str, Any]:
    from ..config.models import WorkspaceSpec

    _validate_name(name)

    # Validation des recipe IDs (avant tout accès disque)
    for rid in req.recipes:
        if not _RECIPE_ID_RE.fullmatch(rid):
            raise HTTPException(
                status_code=422,
                detail=f"Invalid recipe id {rid!r}",
            )

    # Résolution des recettes et de leurs secrets
    resolved_recipes: list[RecipeMeta] = []
    feature_env: dict[str, str] = {}

    if req.recipes:
        reg = _get_recipe_registry()
        shared = await asyncio.to_thread(reg.load_shared)
        personal_dir = safe_user_path(user.login, "recipes")
        personal = await asyncio.to_thread(reg.load_dir, personal_dir)
        available = {**shared, **personal}

        try:
            resolved_recipes = reg.resolve_order(req.recipes, available)
        except KeyError as exc:
            raise HTTPException(status_code=422, detail=str(exc)) from exc
        except ValueError as exc:
            raise HTTPException(status_code=422, detail=str(exc)) from exc

        all_refs: list[SecretRef] = [
            ref for recipe in resolved_recipes for ref in recipe.requires_secrets
        ]
        if all_refs:
            try:
                feature_env = await asyncio.to_thread(
                    _resolve_feature_secrets, user.login, all_refs
                )
            except Exception as exc:
                raise HTTPException(
                    status_code=422,
                    detail=f"Secret resolution failed: {type(exc).__name__}: {exc}",
                ) from exc

    try:
        ws = WorkspaceSpec(name=name, source=req.source, host=req.host, recipes=req.recipes)
    except Exception as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

    svc = _get_service()
    try:
        ws_id = await svc.up(
            login=user.login,
            ws_spec=ws,
            recipes=resolved_recipes or None,
            feature_env=feature_env or None,
        )
    except UnknownHostError as exc:
        raise HTTPException(status_code=404, detail=str(exc)) from exc
    except ValueError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

    _log.info("workspace_up_requested", login=user.login, ws_id=ws_id)
    return {"ws_id": ws_id, "status": "provisioning"}


@router.post("/workspaces/{name}/stop")
async def workspace_stop(
    name: str,
    user: UserInfo = Depends(require_user),
) -> dict[str, Any]:
    _validate_name(name)
    ws_id = f"{user.login}-{name}"
    svc = _get_service()
    await svc.stop(login=user.login, ws_id=ws_id)
    return {"ws_id": ws_id, "status": "stopped"}


@router.post("/workspaces/{name}/delete")
async def workspace_delete(
    name: str,
    user: UserInfo = Depends(require_user),
) -> dict[str, Any]:
    _validate_name(name)
    ws_id = f"{user.login}-{name}"
    svc = _get_service()
    await svc.delete(login=user.login, ws_id=ws_id)
    return {"ws_id": ws_id, "deleted": True}


@router.get("/workspaces/{name}/status")
async def workspace_status(
    name: str,
    user: UserInfo = Depends(require_user),
) -> dict[str, Any]:
    _validate_name(name)
    ws_id = f"{user.login}-{name}"
    svc = _get_service()
    return await svc.status(login=user.login, ws_id=ws_id)
```

- [ ] **Step 5 : Lancer les tests**

```
cd backend && uv run pytest tests/recipes/test_devcontainer_gen.py tests/routes/test_workspace_ops.py -v
```
Attendu : tous PASSED (adapter si besoin les tests existants de test_workspace_ops.py qui appellent `_reset_service()`)

Note : `_reset_service()` remet maintenant aussi `_recipe_registry = None`. Vérifier que les tests existants appellent `_reset_service()` (ils le font déjà via conftest ou directement).

- [ ] **Step 6 : Suite de tests complète**

```
cd backend && uv run pytest -v
```
Attendu : aucune régression.

- [ ] **Step 7 : Lint + mypy**

```
cd backend && uv run ruff check src/portal/devpod/service.py src/portal/routes/workspace_ops.py
cd backend && uv run mypy src/portal/devpod/service.py src/portal/routes/workspace_ops.py
```

- [ ] **Step 8 : Commit**

```
git add backend/src/portal/devpod/service.py backend/src/portal/routes/workspace_ops.py \
        backend/tests/recipes/test_devcontainer_gen.py
git commit -m "feat(recipes): intégrer recettes dans la génération devcontainer (§D-21)"
```

---

### Task 4 : Features builtin — claude-code et les 6 autres CLIs

**Files:**
- Create: `backend/src/portal/recipes/builtin/claude-code/devcontainer-feature.json`
- Create: `backend/src/portal/recipes/builtin/claude-code/install.sh`
- Create: `backend/src/portal/recipes/builtin/claude-code/recipe.meta.yaml`
- (Idem pour : aider, gemini-cli, codex, goose, opencode, cursor-agent)

**Règle critique** : `install.sh` installe le binaire/CLI uniquement. La clé API vient de `requires_secrets` injectée au runtime par le portail — JAMAIS hardcodée dans l'image.

**Note sur les commandes d'installation** : les CLIs évoluent vite. L'agent doit vérifier les commandes courantes (npm info, pip show) avant de committer. Les commandes ci-dessous sont les dernières connues — les valider dans un conteneur jetable :

```bash
docker run --rm ubuntu:24.04 bash -c "apt-get update -qq && npm install -g @anthropic-ai/claude-code && claude --version"
```

- [ ] **Step 1 : Créer les répertoires builtin**

```bash
mkdir -p backend/src/portal/recipes/builtin/{claude-code,aider,gemini-cli,codex,goose,opencode,cursor-agent}
```

- [ ] **Step 2 : Écrire les fichiers pour `claude-code`**

```json
// backend/src/portal/recipes/builtin/claude-code/devcontainer-feature.json
{
  "id": "claude-code",
  "version": "1.0.0",
  "name": "Claude Code CLI",
  "description": "Installe Claude Code (Anthropic CLI) dans le conteneur.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version npm à installer (ex: latest, 0.2.x)"
    }
  },
  "installsAfter": ["ghcr.io/devcontainers/features/node:1"]
}
```

```bash
#!/usr/bin/env bash
# backend/src/portal/recipes/builtin/claude-code/install.sh
# Installe Claude Code CLI (Anthropic).
# La clé API (ANTHROPIC_API_KEY) est injectée au runtime via remoteEnv — jamais ici.
set -euo pipefail

VERSION="${VERSION:-latest}"

# Node.js est requis — installsAfter node feature le garantit
if ! command -v npm &>/dev/null; then
    echo "ERROR: npm not found. Add the node devcontainer feature before claude-code." >&2
    exit 1
fi

echo "==> Installing Claude Code CLI (version: ${VERSION})"
if [[ "$VERSION" == "latest" ]]; then
    npm install -g @anthropic-ai/claude-code
else
    npm install -g "@anthropic-ai/claude-code@${VERSION}"
fi

echo "==> Claude Code installed: $(claude --version 2>/dev/null || echo 'version check skipped')"
```

```yaml
# backend/src/portal/recipes/builtin/claude-code/recipe.meta.yaml
id: claude-code
version: "1.0.0"
description: "Claude Code CLI (Anthropic) — auth via ANTHROPIC_API_KEY au runtime"
options:
  version:
    type: string
    default: "latest"
    description: "Version npm (@anthropic-ai/claude-code)"
requires_secrets:
  - path: "llm/anthropic_key"
    env: "ANTHROPIC_API_KEY"
installs_after:
  - node
```

- [ ] **Step 3 : Écrire les fichiers pour `aider`**

```json
// backend/src/portal/recipes/builtin/aider/devcontainer-feature.json
{
  "id": "aider",
  "version": "1.0.0",
  "name": "Aider AI pair programmer",
  "description": "Installe aider-chat dans le conteneur.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version pip (ex: latest, 0.50.0)"
    }
  },
  "installsAfter": []
}
```

```bash
#!/usr/bin/env bash
# backend/src/portal/recipes/builtin/aider/install.sh
set -euo pipefail
VERSION="${VERSION:-latest}"
echo "==> Installing aider-chat"
if [[ "$VERSION" == "latest" ]]; then
    pip install --quiet aider-chat
else
    pip install --quiet "aider-chat==${VERSION}"
fi
echo "==> aider installed: $(aider --version 2>/dev/null || echo 'ok')"
```

```yaml
# backend/src/portal/recipes/builtin/aider/recipe.meta.yaml
id: aider
version: "1.0.0"
description: "Aider — AI pair programmer"
options:
  version:
    type: string
    default: "latest"
requires_secrets:
  - path: "llm/anthropic_key"
    env: "ANTHROPIC_API_KEY"
installs_after: []
```

- [ ] **Step 4 : Écrire les fichiers pour `gemini-cli`**

```json
// backend/src/portal/recipes/builtin/gemini-cli/devcontainer-feature.json
{
  "id": "gemini-cli",
  "version": "1.0.0",
  "name": "Gemini CLI (Google)",
  "description": "Installe Gemini CLI dans le conteneur.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest"
    }
  },
  "installsAfter": ["ghcr.io/devcontainers/features/node:1"]
}
```

```bash
#!/usr/bin/env bash
# backend/src/portal/recipes/builtin/gemini-cli/install.sh
set -euo pipefail
VERSION="${VERSION:-latest}"
if ! command -v npm &>/dev/null; then
    echo "ERROR: npm not found." >&2; exit 1
fi
echo "==> Installing Gemini CLI"
if [[ "$VERSION" == "latest" ]]; then
    npm install -g @google/gemini-cli
else
    npm install -g "@google/gemini-cli@${VERSION}"
fi
echo "==> Gemini CLI installed"
```

```yaml
# backend/src/portal/recipes/builtin/gemini-cli/recipe.meta.yaml
id: gemini-cli
version: "1.0.0"
description: "Gemini CLI (Google) — auth via GEMINI_API_KEY au runtime"
options:
  version:
    type: string
    default: "latest"
requires_secrets:
  - path: "llm/gemini_key"
    env: "GEMINI_API_KEY"
installs_after:
  - node
```

- [ ] **Step 5 : Écrire les fichiers pour `codex`**

```json
// backend/src/portal/recipes/builtin/codex/devcontainer-feature.json
{
  "id": "codex",
  "version": "1.0.0",
  "name": "OpenAI Codex CLI",
  "description": "Installe OpenAI Codex CLI dans le conteneur.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest"
    }
  },
  "installsAfter": ["ghcr.io/devcontainers/features/node:1"]
}
```

```bash
#!/usr/bin/env bash
# backend/src/portal/recipes/builtin/codex/install.sh
set -euo pipefail
VERSION="${VERSION:-latest}"
if ! command -v npm &>/dev/null; then
    echo "ERROR: npm not found." >&2; exit 1
fi
echo "==> Installing OpenAI Codex CLI"
if [[ "$VERSION" == "latest" ]]; then
    npm install -g @openai/codex
else
    npm install -g "@openai/codex@${VERSION}"
fi
echo "==> Codex CLI installed"
```

```yaml
# backend/src/portal/recipes/builtin/codex/recipe.meta.yaml
id: codex
version: "1.0.0"
description: "OpenAI Codex CLI — auth via OPENAI_API_KEY au runtime"
options:
  version:
    type: string
    default: "latest"
requires_secrets:
  - path: "llm/openai_key"
    env: "OPENAI_API_KEY"
installs_after:
  - node
```

- [ ] **Step 6 : Écrire les fichiers pour `goose`**

```json
// backend/src/portal/recipes/builtin/goose/devcontainer-feature.json
{
  "id": "goose",
  "version": "1.0.0",
  "name": "Goose AI agent (Block)",
  "description": "Installe Goose CLI dans le conteneur.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest"
    }
  },
  "installsAfter": []
}
```

```bash
#!/usr/bin/env bash
# backend/src/portal/recipes/builtin/goose/install.sh
# IMPORTANT: vérifier la commande d'installation actuelle avant de déployer.
# https://block.github.io/goose/docs/installation
set -euo pipefail
echo "==> Installing Goose CLI"
curl -fsSL https://github.com/block/goose/releases/latest/download/goose-installer.sh | bash
echo "==> Goose installed: $(goose --version 2>/dev/null || echo 'ok')"
```

```yaml
# backend/src/portal/recipes/builtin/goose/recipe.meta.yaml
id: goose
version: "1.0.0"
description: "Goose CLI (Block AI agent)"
options:
  version:
    type: string
    default: "latest"
requires_secrets:
  - path: "llm/anthropic_key"
    env: "ANTHROPIC_API_KEY"
installs_after: []
```

- [ ] **Step 7 : Écrire les fichiers pour `opencode`**

```json
// backend/src/portal/recipes/builtin/opencode/devcontainer-feature.json
{
  "id": "opencode",
  "version": "1.0.0",
  "name": "OpenCode CLI",
  "description": "Installe OpenCode CLI dans le conteneur.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest"
    }
  },
  "installsAfter": ["ghcr.io/devcontainers/features/node:1"]
}
```

```bash
#!/usr/bin/env bash
# backend/src/portal/recipes/builtin/opencode/install.sh
# IMPORTANT: vérifier la commande d'installation actuelle (https://opencode.ai/docs)
set -euo pipefail
VERSION="${VERSION:-latest}"
if ! command -v npm &>/dev/null; then
    echo "ERROR: npm not found." >&2; exit 1
fi
echo "==> Installing OpenCode CLI"
if [[ "$VERSION" == "latest" ]]; then
    npm install -g opencode-ai
else
    npm install -g "opencode-ai@${VERSION}"
fi
echo "==> OpenCode installed"
```

```yaml
# backend/src/portal/recipes/builtin/opencode/recipe.meta.yaml
id: opencode
version: "1.0.0"
description: "OpenCode CLI"
options:
  version:
    type: string
    default: "latest"
requires_secrets:
  - path: "llm/anthropic_key"
    env: "ANTHROPIC_API_KEY"
installs_after:
  - node
```

- [ ] **Step 8 : Écrire les fichiers pour `cursor-agent`**

```json
// backend/src/portal/recipes/builtin/cursor-agent/devcontainer-feature.json
{
  "id": "cursor-agent",
  "version": "1.0.0",
  "name": "Cursor Agent (headless)",
  "description": "Installe le Cursor agent headless dans le conteneur.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest"
    }
  },
  "installsAfter": ["ghcr.io/devcontainers/features/node:1"]
}
```

```bash
#!/usr/bin/env bash
# backend/src/portal/recipes/builtin/cursor-agent/install.sh
# IMPORTANT: vérifier la commande d'installation actuelle avant de déployer.
# cursor-agent = CLI headless, pas l'éditeur desktop (inutile en VS Code-in-browser).
set -euo pipefail
VERSION="${VERSION:-latest}"
if ! command -v npm &>/dev/null; then
    echo "ERROR: npm not found." >&2; exit 1
fi
echo "==> Installing Cursor Agent (headless)"
if [[ "$VERSION" == "latest" ]]; then
    npm install -g @cursor/agent
else
    npm install -g "@cursor/agent@${VERSION}"
fi
echo "==> Cursor Agent installed"
```

```yaml
# backend/src/portal/recipes/builtin/cursor-agent/recipe.meta.yaml
id: cursor-agent
version: "1.0.0"
description: "Cursor Agent CLI headless"
options:
  version:
    type: string
    default: "latest"
requires_secrets:
  - path: "llm/openai_key"
    env: "OPENAI_API_KEY"
installs_after:
  - node
```

- [ ] **Step 9 : Test de chargement du registre avec builtin**

```python
# À ajouter dans tests/recipes/test_registry.py

def test_load_builtin_dir_loads_real_recipes() -> None:
    """Le répertoire builtin réel contient au moins claude-code."""
    from portal.recipes.registry import BUILTIN_DIR, RecipeRegistry

    if not BUILTIN_DIR.exists():
        pytest.skip("BUILTIN_DIR non créé (Task 4 pas encore faite)")
    registry = RecipeRegistry()
    shared = registry.load_shared()
    assert "claude-code" in shared
    assert shared["claude-code"].requires_secrets[0].env == "ANTHROPIC_API_KEY"
```

- [ ] **Step 10 : Lancer les tests**

```
cd backend && uv run pytest tests/recipes/ -v
```
Attendu : tous PASSED

- [ ] **Step 11 : Commit**

```
git add backend/src/portal/recipes/builtin/
git commit -m "feat(recipes): ajouter 7 Features builtin (claude-code, aider, gemini-cli, codex, goose, opencode, cursor-agent)"
```

---

### Task 5 : Endpoints REST `/recipes` et `/admin/recipes`

**Files:**
- Create: `backend/src/portal/routes/recipes.py`
- Modify: `backend/src/portal/app.py`
- Create: `backend/tests/routes/test_recipes.py`

**Endpoints :**
- `GET /recipes` — liste shared + personal pour l'user connecté
- `GET /me/recipes/{recipe_id}` — détail d'une recette perso
- `DELETE /me/recipes/{recipe_id}` — supprime une recette perso
- `GET /admin/recipes` — liste partagées (admin only)
- `DELETE /admin/recipes/{recipe_id}` — supprime une partagée (admin only)

**Note :** L'upload de recettes (POST) implique plusieurs fichiers (json + sh + yaml). Pour M7, upload = déployer manuellement via volume ou scp. Les endpoints DELETE et GET sont suffisants pour la Definition of Done.

- [ ] **Step 1 : Écrire les tests qui échouent**

```python
# backend/tests/routes/test_recipes.py
from __future__ import annotations

import os
import yaml
from pathlib import Path

import pytest
from fastapi.testclient import TestClient


def _write_global_config(tmp_path: Path) -> None:
    config = {
        "version": "1",
        "server": {
            "listen": "0.0.0.0:8080",
            "base_domain": "dev.yoops.org",
            "external_url": "https://dev.yoops.org",
            "dev_mode": True,
            "log": {"level": "info", "format": "text", "output": ""},
        },
        "auth": {
            "oidc": {
                "issuer": "https://kc.test",
                "client_id": "portal",
                "client_secret": "",
                "scopes": ["openid"],
                "role_claim": "realm_access.roles",
                "admin_role": "admin",
                "user_role": "dev",
                "username_claim": "preferred_username",
            }
        },
        "secrets": {
            "backend": "inline",
            "harpocrate": {"url": "", "api_key": "", "base_path": "devpod"},
        },
        "devpod": {
            "binary": "devpod",
            "defaults": {"ide": "openvscode", "idle_timeout": "2h", "dotfiles": ""},
            "client_cert_path": "/data/certs/portal",
        },
        "hosts": [],
        "caddy": {"admin_api": ""},
        "cloudflare_manager": {"url": "", "api_key": ""},
    }
    (tmp_path / "config.yaml").write_text(
        yaml.dump(config, default_flow_style=False), encoding="utf-8"
    )


def _write_recipe(base: Path, recipe_id: str) -> None:
    import json
    d = base / recipe_id
    d.mkdir(parents=True, exist_ok=True)
    (d / "recipe.meta.yaml").write_text(
        yaml.dump({"id": recipe_id, "version": "1.0.0", "description": f"Recipe {recipe_id}"}),
        encoding="utf-8",
    )
    (d / "devcontainer-feature.json").write_text(
        json.dumps({"id": recipe_id, "version": "1.0.0"}), encoding="utf-8"
    )
    (d / "install.sh").write_text("#!/usr/bin/env bash\necho ok\n", encoding="utf-8")


def _make_app(tmp_path: Path, user_role: str = "dev"):
    import portal.settings as mod
    from portal.routes.workspace_ops import _reset_service

    mod._settings = None
    os.environ["PORTAL_DATA_ROOT"] = str(tmp_path)
    os.environ["SESSION_SECRET_KEY"] = "test-secret-key-32chars-minimum!!"
    mod._settings = None
    _reset_service()

    from portal.app import create_app
    from portal.auth.rbac import UserInfo, require_admin, require_user

    app = create_app()
    app.dependency_overrides[require_user] = lambda: UserInfo(login="alice", roles=[user_role])
    app.dependency_overrides[require_admin] = lambda: UserInfo(login="admin", roles=["admin"])
    return app


def test_get_recipes_returns_shared(tmp_path: Path) -> None:
    _write_global_config(tmp_path)
    shared_dir = tmp_path / "recipes"
    _write_recipe(shared_dir, "claude-code")
    _write_recipe(shared_dir, "aider")
    app = _make_app(tmp_path)
    with TestClient(app) as client:
        resp = client.get("/recipes")
    assert resp.status_code == 200
    data = resp.json()
    ids = [r["id"] for r in data]
    assert "claude-code" in ids
    assert "aider" in ids


def test_get_recipes_includes_personal(tmp_path: Path) -> None:
    _write_global_config(tmp_path)
    from portal.config.store import ensure_user_dir

    ensure_user_dir("alice")
    personal_dir = tmp_path / "users" / "alice" / "recipes"
    _write_recipe(personal_dir, "my-custom")
    app = _make_app(tmp_path)
    with TestClient(app) as client:
        resp = client.get("/recipes")
    assert resp.status_code == 200
    ids = [r["id"] for r in resp.json()]
    assert "my-custom" in ids


def test_get_recipes_requires_auth(tmp_path: Path) -> None:
    _write_global_config(tmp_path)
    import portal.settings as mod
    from portal.routes.workspace_ops import _reset_service

    mod._settings = None
    os.environ["PORTAL_DATA_ROOT"] = str(tmp_path)
    os.environ["SESSION_SECRET_KEY"] = "test-secret-key-32chars-minimum!!"
    mod._settings = None
    _reset_service()

    from portal.app import create_app

    app = create_app()
    with TestClient(app) as client:
        resp = client.get("/recipes")
    assert resp.status_code == 403


def test_delete_personal_recipe(tmp_path: Path) -> None:
    _write_global_config(tmp_path)
    from portal.config.store import ensure_user_dir

    ensure_user_dir("alice")
    personal_dir = tmp_path / "users" / "alice" / "recipes"
    _write_recipe(personal_dir, "my-custom")
    app = _make_app(tmp_path)
    with TestClient(app) as client:
        resp = client.delete("/me/recipes/my-custom")
    assert resp.status_code == 200
    assert not (personal_dir / "my-custom").exists()


def test_delete_personal_recipe_not_found(tmp_path: Path) -> None:
    _write_global_config(tmp_path)
    app = _make_app(tmp_path)
    with TestClient(app) as client:
        resp = client.delete("/me/recipes/nonexistent")
    assert resp.status_code == 404


def test_admin_get_recipes_shared_only(tmp_path: Path) -> None:
    _write_global_config(tmp_path)
    shared_dir = tmp_path / "recipes"
    _write_recipe(shared_dir, "shared-tool")
    app = _make_app(tmp_path, user_role="admin")
    with TestClient(app) as client:
        resp = client.get("/admin/recipes")
    assert resp.status_code == 200
    ids = [r["id"] for r in resp.json()]
    assert "shared-tool" in ids


def test_admin_delete_shared_recipe(tmp_path: Path) -> None:
    _write_global_config(tmp_path)
    shared_dir = tmp_path / "recipes"
    _write_recipe(shared_dir, "shared-tool")
    app = _make_app(tmp_path, user_role="admin")
    with TestClient(app) as client:
        resp = client.delete("/admin/recipes/shared-tool")
    assert resp.status_code == 200
    assert not (shared_dir / "shared-tool").exists()


def test_delete_shared_recipe_path_traversal_rejected(tmp_path: Path) -> None:
    _write_global_config(tmp_path)
    app = _make_app(tmp_path, user_role="admin")
    with TestClient(app) as client:
        resp = client.delete("/admin/recipes/../etc")
    assert resp.status_code in (404, 422)
```

- [ ] **Step 2 : Lancer les tests, vérifier qu'ils échouent**

```
cd backend && uv run pytest tests/routes/test_recipes.py -v
```
Attendu : FAIL (404 ou ImportError — router pas encore créé)

- [ ] **Step 3 : Créer `backend/src/portal/routes/recipes.py`**

```python
# backend/src/portal/routes/recipes.py
from __future__ import annotations

import re
import shutil
from typing import Any

import structlog
from fastapi import APIRouter, Depends, HTTPException

from ..auth.rbac import UserInfo, require_admin, require_user
from ..config.store import _data_root, safe_user_path
from ..recipes.registry import RecipeRegistry

_log = structlog.get_logger(__name__)

router_user = APIRouter(tags=["recipes"])
router_admin = APIRouter(tags=["recipes-admin"])

_RECIPE_ID_RE = re.compile(r"^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$")


def _validate_recipe_id(recipe_id: str) -> None:
    if not _RECIPE_ID_RE.fullmatch(recipe_id):
        raise HTTPException(
            status_code=422,
            detail=f"Invalid recipe id {recipe_id!r}",
        )


def _safe_shared_recipe_path(recipe_id: str) -> Any:
    _validate_recipe_id(recipe_id)
    data_root = _data_root()
    path = data_root / "recipes" / recipe_id
    if not path.is_relative_to(data_root / "recipes"):
        raise HTTPException(status_code=422, detail="Path traversal detected")
    return path


@router_user.get("/recipes")
async def list_recipes(user: UserInfo = Depends(require_user)) -> list[dict[str, Any]]:
    """Liste les recettes partagées + personnelles visibles par l'utilisateur."""
    data_root = _data_root()
    registry = RecipeRegistry(shared_dir=data_root / "recipes")
    shared = registry.load_shared()
    personal_dir = safe_user_path(user.login, "recipes")
    personal = registry.load_dir(personal_dir)
    available = {**shared, **personal}
    return [m.model_dump() for m in available.values()]


@router_user.delete("/me/recipes/{recipe_id}")
async def delete_personal_recipe(
    recipe_id: str,
    user: UserInfo = Depends(require_user),
) -> dict[str, Any]:
    """Supprime une recette personnelle."""
    _validate_recipe_id(recipe_id)
    personal_dir = safe_user_path(user.login, "recipes")
    recipe_path = personal_dir / recipe_id
    if not recipe_path.is_relative_to(personal_dir):
        raise HTTPException(status_code=422, detail="Path traversal detected")
    if not recipe_path.exists():
        raise HTTPException(status_code=404, detail=f"Recipe {recipe_id!r} not found")
    shutil.rmtree(recipe_path)
    _log.info("personal_recipe_deleted", login=user.login, recipe_id=recipe_id)
    return {"deleted": recipe_id}


@router_admin.get("/recipes")
async def admin_list_recipes(user: UserInfo = Depends(require_admin)) -> list[dict[str, Any]]:
    """Liste les recettes partagées (admin only)."""
    data_root = _data_root()
    registry = RecipeRegistry(shared_dir=data_root / "recipes")
    shared = registry.load_shared()
    return [m.model_dump() for m in shared.values()]


@router_admin.delete("/recipes/{recipe_id}")
async def admin_delete_shared_recipe(
    recipe_id: str,
    user: UserInfo = Depends(require_admin),
) -> dict[str, Any]:
    """Supprime une recette partagée (admin only, ne supprime pas les builtin)."""
    recipe_path = _safe_shared_recipe_path(recipe_id)
    if not recipe_path.exists():
        raise HTTPException(status_code=404, detail=f"Recipe {recipe_id!r} not found")
    shutil.rmtree(recipe_path)
    _log.info("shared_recipe_deleted", login=user.login, recipe_id=recipe_id)
    return {"deleted": recipe_id}
```

- [ ] **Step 4 : Modifier `backend/src/portal/app.py`**

```python
# backend/src/portal/app.py
from __future__ import annotations

import structlog
from fastapi import FastAPI
from starlette.middleware.sessions import SessionMiddleware

from .auth.router import router as auth_router
from .routes.admin import router as admin_router
from .routes.me import router as me_router
from .routes.nodes import router as nodes_router
from .routes.recipes import router_admin as recipes_admin_router
from .routes.recipes import router_user as recipes_user_router
from .routes.workspace_ops import router as workspace_ops_router
from .settings import get_settings

_log = structlog.get_logger(__name__)


def create_app() -> FastAPI:
    settings = get_settings()

    if not settings.session_secret_key:
        if settings.dev_mode:
            _log.warning(
                "session_secret_key_empty_dev_mode",
                msg="SESSION_SECRET_KEY not set — using insecure fallback (dev mode only)",
            )
        else:
            raise RuntimeError(
                "SESSION_SECRET_KEY must be set via environment variable or .env file. "
                "Starting without a session secret key is not allowed in production."
            )

    app = FastAPI(title="workspace-portal", version="0.1.0")
    app.add_middleware(
        SessionMiddleware,
        secret_key=settings.session_secret_key or "dev-only-insecure-key",
        session_cookie="portal_session",
        https_only=not settings.dev_mode,
        same_site="lax",
        max_age=86400,
    )
    app.include_router(auth_router)
    app.include_router(me_router, prefix="/me")
    app.include_router(workspace_ops_router, prefix="/me")
    app.include_router(recipes_user_router)
    app.include_router(recipes_admin_router, prefix="/admin")
    app.include_router(admin_router, prefix="/admin")
    app.include_router(nodes_router, prefix="/admin")
    return app


app = create_app()
```

- [ ] **Step 5 : Lancer les tests**

```
cd backend && uv run pytest tests/routes/test_recipes.py -v
```
Attendu : tous PASSED

- [ ] **Step 6 : Suite complète**

```
cd backend && uv run pytest -v
```
Attendu : aucune régression.

- [ ] **Step 7 : Lint + mypy**

```
cd backend && uv run ruff check src/portal/routes/recipes.py src/portal/app.py tests/routes/test_recipes.py
cd backend && uv run mypy src/portal/routes/recipes.py src/portal/app.py
```

- [ ] **Step 8 : Commit**

```
git add backend/src/portal/routes/recipes.py backend/src/portal/app.py \
        backend/tests/routes/test_recipes.py
git commit -m "feat(recipes): ajouter endpoints REST GET /recipes + DELETE /me|admin/recipes"
```

---

## Definition of Done

- [ ] `uv run pytest -v` → tous les tests passent (0 erreurs, 0 warnings critiques)
- [ ] `uv run ruff check src/ tests/` → 0 erreur
- [ ] `uv run mypy src/` → 0 erreur
- [ ] `GET /recipes` retourne au moins `claude-code` (builtin)
- [ ] Un workspace créé avec `recipes: ["claude-code"]` génère un devcontainer.json avec `features: {"./claude-code": {}}` et `remoteEnv: {"ANTHROPIC_API_KEY": "..."}` — assert sur le JSON généré (test_devcontainer_gen.py)
- [ ] §D-21 vérifié : assert que `"sk-secret"` n'apparaît PAS dans la clé `features` du JSON (test_devcontainer_gen.py::test_write_devcontainer_secrets_in_remote_env_not_features)
- [ ] `DELETE /me/recipes/nonexistent` → 404 (test_recipes.py)
- [ ] Path traversal sur recipe_id → 422/404 (test_recipes.py::test_delete_shared_recipe_path_traversal_rejected)

## Pièges spécifiques M7

- **§D-21** : ne jamais passer les valeurs de secrets dans `features.{id}` options — seulement dans `remoteEnv`.
- **Nettoyage tmpdir** : `_run_up_task` doit utiliser `shutil.rmtree(dc_path.parent)` et non `dc_path.unlink()` — sinon les Feature dirs copiées restent sur disque.
- **Cycle Kahn** : le tri topologique ne traite que le sous-ensemble sélectionné — une dépendance `installs_after` vers une recette NON sélectionnée est ignorée (pas une erreur).
- **Recette manquante dans builtin** : si la Feature dir n'existe pas pour un recipe.id, `_write_devcontainer` l'ignore silencieusement (la Feature n'est pas copiée, pas de crash) — testé dans `test_write_devcontainer_cleanup_on_error`.
- **`_reset_service()` réinitialise aussi `_recipe_registry`** — sinon les tests de `test_recipes.py` qui changent `PORTAL_DATA_ROOT` ne voient pas les bons répertoires.
- **Commandes install.sh** : ne pas figer une commande sans la tester. Les noms de packages npm/pip changent. Valider dans un conteneur jetable (`docker run --rm ubuntu:24.04 bash -c "..."`) avant de committer les recettes.
