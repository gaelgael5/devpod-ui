# M2 — API, OIDC Keycloak, RBAC, provisioning user

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Application FastAPI avec authentification OIDC contre Keycloak, RBAC dev/admin, provisioning automatique du répertoire user au premier login, et endpoints CRUD de configuration.

**Architecture:** FastAPI + SessionMiddleware (cookie signé). OIDCClient gère le flow code + PKCE + state + nonce + validation JWKS. RBAC via dépendances FastAPI. Pas de DB — YAML atomique via M1.

**Tech Stack:** Python 3.12, fastapi[standard]>=0.111, authlib>=1.3, itsdangerous>=2.1, httpx, pydantic-settings, structlog, pytest + respx (mock httpx).

---

## Structure des fichiers

```
backend/src/portal/
├── app.py                    # (modifié) create_app(), lifespan, SessionMiddleware, routers
├── settings.py               # (nouveau) AppSettings via pydantic-settings
├── config/
│   └── store.py              # (modifié) _LOGIN_RE étendu aux points, save_global()
├── auth/
│   ├── __init__.py           # (nouveau) vide
│   ├── jwks.py               # (nouveau) JWKSCache : async, TTL, refetch sur kid inconnu
│   ├── oidc.py               # (nouveau) OIDCClient : discover, authorization_url, exchange_and_validate
│   ├── router.py             # (nouveau) /auth/login, /auth/callback, /auth/logout
│   └── rbac.py               # (nouveau) UserInfo, get_current_user, require_user, require_admin
└── routes/
    ├── __init__.py           # (nouveau) vide
    ├── me.py                 # (nouveau) GET/PUT /me/config, GET/POST/DELETE /me/workspaces
    └── admin.py              # (nouveau) GET/PUT /admin/config, GET/POST /admin/hosts

backend/tests/
├── auth/
│   ├── __init__.py
│   ├── conftest.py           # RSA keypair, fake token factory, mock discovery doc
│   ├── test_jwks.py          # JWKSCache : hit, miss, refetch
│   ├── test_oidc.py          # OIDCClient : authorization_url, validate_id_token, state mismatch
│   ├── test_router.py        # /auth/login redirect, /auth/callback flow, provisioning
│   └── test_rbac.py          # require_user/admin, extraction rôles, username invalide
└── routes/
    ├── __init__.py
    ├── test_me.py            # GET/PUT /me/config, GET/POST/DELETE /me/workspaces
    └── test_admin.py         # GET/PUT /admin/config, require_admin (403 dev, 200 admin)
```

---

## Task 0 : Dépendances M2

**Files:**
- Modify: `backend/pyproject.toml`

- [ ] **Step 1 : Ajouter les dépendances dans pyproject.toml**

Remplacer la section `[project]` dependencies par :

```toml
[project]
name = "portal"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "pyyaml>=6.0",
    "httpx>=0.27",
    "structlog>=24.0",
    "fastapi[standard]>=0.111",
    "authlib>=1.3",
    "itsdangerous>=2.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.4",
    "mypy>=1.10",
    "types-pyyaml",
    "respx>=0.21",
]
```

- [ ] **Step 2 : Synchroniser l'environnement**

```bash
cd backend && uv sync
```

Attendu : aucune erreur, fastapi, authlib, itsdangerous, respx installés.

- [ ] **Step 3 : Vérifier que pytest tourne toujours**

```bash
cd backend && uv run pytest -q
```

Attendu : `65 passed`

- [ ] **Step 4 : Commit**

```bash
git add backend/pyproject.toml backend/uv.lock
git commit -m "chore(deps): ajoute fastapi, authlib, itsdangerous, respx pour M2"
```

---

## Task 1 : AppSettings + squelette app.py

**Files:**
- Create: `backend/src/portal/settings.py`
- Create: `backend/src/portal/app.py`

- [ ] **Step 1 : Écrire le test rouge (settings)**

Créer `backend/tests/test_settings.py` :

```python
from __future__ import annotations
import pytest


def test_settings_default_values(monkeypatch: pytest.MonkeyPatch) -> None:
    """Les settings ont des valeurs par défaut raisonnables."""
    monkeypatch.delenv("SESSION_SECRET_KEY", raising=False)
    monkeypatch.delenv("OIDC_ISSUER", raising=False)
    # réimporter pour contourner le cache
    import importlib
    import portal.settings as mod
    mod._settings = None  # reset cache
    settings = mod.get_settings()
    assert settings.oidc_leeway == 30
    assert settings.session_secret_key == ""


def test_settings_reads_env(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("SESSION_SECRET_KEY", "supersecret")
    monkeypatch.setenv("OIDC_ISSUER", "https://kc.example.com/realms/test")
    import portal.settings as mod
    mod._settings = None
    settings = mod.get_settings()
    assert settings.session_secret_key == "supersecret"
    assert settings.oidc_issuer == "https://kc.example.com/realms/test"
    mod._settings = None  # cleanup
```

- [ ] **Step 2 : Exécuter le test pour vérifier qu'il échoue**

```bash
cd backend && uv run pytest tests/test_settings.py -v
```

Attendu : FAIL (ImportError ou AttributeError)

- [ ] **Step 3 : Créer `src/portal/settings.py`**

```python
from __future__ import annotations

from pydantic_settings import BaseSettings, SettingsConfigDict


class AppSettings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore",
    )

    # Session (cookie signé)
    session_secret_key: str = ""

    # OIDC
    oidc_issuer: str = ""
    oidc_client_id: str = ""
    oidc_client_secret: str = ""
    oidc_redirect_uri: str = "https://dev.yoops.org/auth/callback"
    oidc_role_claim: str = "realm_access.roles"
    oidc_admin_role: str = "admin"
    oidc_user_role: str = "dev"
    oidc_username_claim: str = "preferred_username"
    oidc_leeway: int = 30  # secondes

    # Data root (surchargé en test via PORTAL_DATA_ROOT)
    portal_data_root: str = "/data"


_settings: AppSettings | None = None


def get_settings() -> AppSettings:
    global _settings
    if _settings is None:
        _settings = AppSettings()
    return _settings
```

- [ ] **Step 4 : Créer `src/portal/app.py`**

```python
from __future__ import annotations

from fastapi import FastAPI
from starlette.middleware.sessions import SessionMiddleware

from .settings import get_settings


def create_app() -> FastAPI:
    settings = get_settings()
    app = FastAPI(title="workspace-portal", version="0.1.0")
    app.add_middleware(
        SessionMiddleware,
        secret_key=settings.session_secret_key or "dev-only-insecure-key",
        session_cookie="portal_session",
        https_only=False,   # mis à True en prod via config
        same_site="lax",
        max_age=86400,
    )
    return app


app = create_app()
```

Note : `https_only=False` en développement (TestClient ne fait pas TLS). En production, ce flag sera contrôlé par un setting.

- [ ] **Step 5 : Vérifier les tests**

```bash
cd backend && uv run pytest tests/test_settings.py -v
```

Attendu : `2 passed`

- [ ] **Step 6 : Vérifier lint + mypy**

```bash
cd backend && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/ && uv run mypy src/
```

Attendu : 0 erreurs

- [ ] **Step 7 : Commit**

```bash
git add backend/src/portal/settings.py backend/src/portal/app.py backend/tests/test_settings.py
git commit -m "feat(app): AppSettings pydantic-settings + squelette FastAPI avec SessionMiddleware"
```

---

## Task 2 : JWKSCache

**Files:**
- Create: `backend/src/portal/auth/__init__.py`
- Create: `backend/src/portal/auth/jwks.py`
- Create: `backend/tests/auth/__init__.py`
- Create: `backend/tests/auth/conftest.py`
- Create: `backend/tests/auth/test_jwks.py`

- [ ] **Step 1 : Écrire les tests rouges**

Créer `backend/tests/auth/conftest.py` :

```python
from __future__ import annotations

import json
import time
import pytest
from authlib.jose import OctKey, RSAKey, jwt as jose_jwt
from authlib.jose import JsonWebKeySet


RSA_KEY = RSAKey.generate_key(2048, is_private=True)
TEST_KID = "test-key-id"


def make_jwks_response(key: RSAKey = RSA_KEY, kid: str = TEST_KID) -> dict:
    """Retourne un dict JWKS avec la clé publique."""
    pub = key.as_dict(is_private=False)
    pub["kid"] = kid
    pub["use"] = "sig"
    pub["alg"] = "RS256"
    return {"keys": [pub]}


def make_id_token(
    *,
    issuer: str,
    client_id: str,
    subject: str = "user-sub-123",
    username: str = "alice",
    roles: list[str] | None = None,
    nonce: str = "test-nonce",
    expiry_offset: int = 300,
    kid: str = TEST_KID,
    key: RSAKey = RSA_KEY,
) -> str:
    """Signe un ID token avec la clé RSA de test."""
    now = int(time.time())
    payload = {
        "iss": issuer,
        "aud": client_id,
        "sub": subject,
        "iat": now,
        "exp": now + expiry_offset,
        "nonce": nonce,
        "preferred_username": username,
        "realm_access": {"roles": roles or ["dev"]},
    }
    headers = {"alg": "RS256", "kid": kid}
    token = jose_jwt.encode(headers, payload, key)
    return token.decode() if isinstance(token, bytes) else token


ISSUER = "https://kc.test/realms/yoops"
CLIENT_ID = "workspace-portal"
DISCOVERY_DOC = {
    "issuer": ISSUER,
    "authorization_endpoint": f"{ISSUER}/protocol/openid-connect/auth",
    "token_endpoint": f"{ISSUER}/protocol/openid-connect/token",
    "jwks_uri": f"{ISSUER}/protocol/openid-connect/certs",
    "response_types_supported": ["code"],
}
```

Créer `backend/tests/auth/test_jwks.py` :

```python
from __future__ import annotations

import pytest
import respx
import httpx

from tests.auth.conftest import make_jwks_response, ISSUER


JWKS_URI = f"{ISSUER}/protocol/openid-connect/certs"


@pytest.mark.asyncio
async def test_jwks_cache_fetches_on_first_call() -> None:
    from portal.auth.jwks import JWKSCache

    with respx.mock:
        respx.get(JWKS_URI).mock(return_value=httpx.Response(200, json=make_jwks_response()))
        cache = JWKSCache(JWKS_URI)
        keyset = await cache.get_keyset()
        assert keyset is not None
        assert len(respx.calls) == 1


@pytest.mark.asyncio
async def test_jwks_cache_does_not_refetch_within_ttl() -> None:
    from portal.auth.jwks import JWKSCache

    with respx.mock:
        respx.get(JWKS_URI).mock(return_value=httpx.Response(200, json=make_jwks_response()))
        cache = JWKSCache(JWKS_URI, ttl=3600)
        await cache.get_keyset()
        await cache.get_keyset()  # doit utiliser le cache
        assert len(respx.calls) == 1


@pytest.mark.asyncio
async def test_jwks_cache_refetches_when_forced() -> None:
    from portal.auth.jwks import JWKSCache

    with respx.mock:
        respx.get(JWKS_URI).mock(return_value=httpx.Response(200, json=make_jwks_response()))
        cache = JWKSCache(JWKS_URI, ttl=3600)
        await cache.get_keyset()
        await cache.get_keyset(refetch=True)
        assert len(respx.calls) == 2


@pytest.mark.asyncio
async def test_jwks_cache_raises_on_http_error() -> None:
    from portal.auth.jwks import JWKSCache

    with respx.mock:
        respx.get(JWKS_URI).mock(return_value=httpx.Response(503))
        cache = JWKSCache(JWKS_URI)
        with pytest.raises(httpx.HTTPStatusError):
            await cache.get_keyset()
```

- [ ] **Step 2 : Vérifier que les tests échouent**

```bash
cd backend && uv run pytest tests/auth/test_jwks.py -v
```

Attendu : FAIL (ImportError)

- [ ] **Step 3 : Créer `src/portal/auth/__init__.py`** (vide)

- [ ] **Step 4 : Créer `src/portal/auth/jwks.py`**

```python
from __future__ import annotations

import time
import structlog
import httpx
from authlib.jose import JsonWebKeySet

_log = structlog.get_logger(__name__)


class JWKSCache:
    """Cache de JWKS avec TTL et refetch forcé sur kid inconnu."""

    def __init__(self, jwks_uri: str, ttl: int = 3600) -> None:
        self._uri = jwks_uri
        self._ttl = ttl
        self._keyset: JsonWebKeySet | None = None
        self._fetched_at: float = 0.0

    async def get_keyset(self, *, refetch: bool = False) -> JsonWebKeySet:
        now = time.monotonic()
        stale = self._keyset is None or (now - self._fetched_at) > self._ttl
        if refetch or stale:
            await self._fetch()
        assert self._keyset is not None
        return self._keyset

    async def _fetch(self) -> None:
        _log.info("jwks_fetch", uri=self._uri)
        async with httpx.AsyncClient() as client:
            resp = await client.get(self._uri, timeout=10.0)
            resp.raise_for_status()
        self._keyset = JsonWebKeySet.import_key_set(resp.json())
        self._fetched_at = time.monotonic()
        _log.info("jwks_fetched", key_count=len(list(self._keyset.keys)))
```

- [ ] **Step 5 : Vérifier les tests**

```bash
cd backend && uv run pytest tests/auth/test_jwks.py -v
```

Attendu : `4 passed`

- [ ] **Step 6 : Lint + mypy**

```bash
cd backend && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/ && uv run mypy src/
```

- [ ] **Step 7 : Commit**

```bash
git add backend/src/portal/auth/ backend/tests/auth/
git commit -m "feat(auth): JWKSCache async avec TTL et refetch forcé"
```

---

## Task 3 : OIDCClient

**Files:**
- Create: `backend/src/portal/auth/oidc.py`
- Create: `backend/tests/auth/test_oidc.py`

- [ ] **Step 1 : Écrire les tests rouges**

Créer `backend/tests/auth/test_oidc.py` :

```python
from __future__ import annotations

import time
import pytest
import respx
import httpx

from tests.auth.conftest import (
    make_id_token,
    make_jwks_response,
    ISSUER,
    CLIENT_ID,
    DISCOVERY_DOC,
    RSA_KEY,
    TEST_KID,
)


def _make_client():
    from portal.auth.oidc import OIDCClient
    return OIDCClient(
        issuer=ISSUER,
        client_id=CLIENT_ID,
        client_secret="secret",
        redirect_uri="https://portal.test/auth/callback",
        leeway=30,
    )


JWKS_URI = DISCOVERY_DOC["jwks_uri"]
TOKEN_ENDPOINT = DISCOVERY_DOC["token_endpoint"]


@pytest.mark.asyncio
async def test_authorization_url_contains_pkce_and_state() -> None:
    """authorization_url génère state, nonce, code_challenge dans la session."""
    with respx.mock:
        respx.get(f"{ISSUER}/.well-known/openid-configuration").mock(
            return_value=httpx.Response(200, json=DISCOVERY_DOC)
        )
        client = _make_client()
        session: dict = {}
        url = await client.authorization_url(session)

    assert "code_challenge=" in url
    assert "code_challenge_method=S256" in url
    assert f"state={session['oidc_state']}" in url
    assert f"nonce={session['oidc_nonce']}" in url
    assert session["oidc_verifier"]  # non vide


@pytest.mark.asyncio
async def test_exchange_raises_on_state_mismatch() -> None:
    from portal.auth.oidc import OIDCError

    with respx.mock:
        respx.get(f"{ISSUER}/.well-known/openid-configuration").mock(
            return_value=httpx.Response(200, json=DISCOVERY_DOC)
        )
        client = _make_client()
        session: dict = {}
        await client.authorization_url(session)

    with pytest.raises(OIDCError, match="state mismatch"):
        await client.exchange_and_validate(code="any", state="WRONG", session=session)


@pytest.mark.asyncio
async def test_exchange_validates_id_token_successfully() -> None:
    """Callback complet : échange du code, validation du token, retour des claims."""
    nonce = "my-nonce"
    id_token = make_id_token(
        issuer=ISSUER,
        client_id=CLIENT_ID,
        nonce=nonce,
        username="alice",
        roles=["dev"],
    )

    with respx.mock:
        respx.get(f"{ISSUER}/.well-known/openid-configuration").mock(
            return_value=httpx.Response(200, json=DISCOVERY_DOC)
        )
        respx.get(JWKS_URI).mock(
            return_value=httpx.Response(200, json=make_jwks_response())
        )
        respx.post(TOKEN_ENDPOINT).mock(
            return_value=httpx.Response(200, json={"id_token": id_token, "access_token": "acc"})
        )

        client = _make_client()
        session = {
            "oidc_state": "correct-state",
            "oidc_nonce": nonce,
            "oidc_verifier": "abc123",
        }
        claims = await client.exchange_and_validate(
            code="auth-code", state="correct-state", session=session
        )

    assert claims["preferred_username"] == "alice"
    assert "oidc_verifier" not in session  # nettoyé après usage
    assert "oidc_nonce" not in session


@pytest.mark.asyncio
async def test_validate_refetches_jwks_on_unknown_kid() -> None:
    """Si le kid est inconnu, le client refetch le JWKS et réessaie."""
    nonce = "nonce2"
    id_token = make_id_token(
        issuer=ISSUER,
        client_id=CLIENT_ID,
        nonce=nonce,
        kid=TEST_KID,
    )
    # Premier JWKS sans le kid, deuxième avec
    empty_jwks = {"keys": []}
    real_jwks = make_jwks_response()

    with respx.mock:
        respx.get(f"{ISSUER}/.well-known/openid-configuration").mock(
            return_value=httpx.Response(200, json=DISCOVERY_DOC)
        )
        jwks_route = respx.get(JWKS_URI)
        jwks_route.side_effect = [
            httpx.Response(200, json=empty_jwks),
            httpx.Response(200, json=real_jwks),
        ]
        respx.post(TOKEN_ENDPOINT).mock(
            return_value=httpx.Response(200, json={"id_token": id_token, "access_token": "acc"})
        )

        client = _make_client()
        session = {
            "oidc_state": "s",
            "oidc_nonce": nonce,
            "oidc_verifier": "v",
        }
        claims = await client.exchange_and_validate(code="c", state="s", session=session)

    assert claims["preferred_username"] == "alice"
    assert len(jwks_route.calls) == 2  # initial + refetch


@pytest.mark.asyncio
async def test_validate_raises_on_expired_token() -> None:
    from portal.auth.oidc import OIDCError

    nonce = "nonce3"
    expired_token = make_id_token(
        issuer=ISSUER,
        client_id=CLIENT_ID,
        nonce=nonce,
        expiry_offset=-3600,  # expiré il y a 1h
    )

    with respx.mock:
        respx.get(f"{ISSUER}/.well-known/openid-configuration").mock(
            return_value=httpx.Response(200, json=DISCOVERY_DOC)
        )
        respx.get(JWKS_URI).mock(
            return_value=httpx.Response(200, json=make_jwks_response())
        )
        respx.post(TOKEN_ENDPOINT).mock(
            return_value=httpx.Response(200, json={"id_token": expired_token, "access_token": "acc"})
        )

        client = _make_client()
        session = {"oidc_state": "s", "oidc_nonce": nonce, "oidc_verifier": "v"}
        with pytest.raises(OIDCError, match="expired"):
            await client.exchange_and_validate(code="c", state="s", session=session)
```

- [ ] **Step 2 : Vérifier que les tests échouent**

```bash
cd backend && uv run pytest tests/auth/test_oidc.py -v
```

Attendu : FAIL (ImportError)

- [ ] **Step 3 : Consulter la doc authlib (context7)**

Avant d'écrire le code, récupérer la doc authlib via context7 pour `authlib.jose` (jwt.decode, JsonWebKeySet, JoseError). Vérifier en particulier : comment decoder un JWT avec une JsonWebKeySet, et comment `JoseError` se subdivise.

- [ ] **Step 4 : Créer `src/portal/auth/oidc.py`**

```python
from __future__ import annotations

import base64
import hashlib
import os
import structlog
import httpx
from authlib.jose import jwt as jose_jwt, JsonWebKeySet
from authlib.jose.errors import JoseError

from .jwks import JWKSCache

_log = structlog.get_logger(__name__)


class OIDCError(Exception):
    """Erreur d'authentification OIDC."""


class OIDCClient:
    """
    Client OIDC : flow code + PKCE + state + nonce.
    Validation ID token avec cache JWKS et refetch sur kid inconnu.
    """

    def __init__(
        self,
        issuer: str,
        client_id: str,
        client_secret: str,
        redirect_uri: str,
        leeway: int = 30,
    ) -> None:
        self._issuer = issuer
        self._client_id = client_id
        self._client_secret = client_secret
        self._redirect_uri = redirect_uri
        self._leeway = leeway
        self._authorization_endpoint = ""
        self._token_endpoint = ""
        self._jwks_cache: JWKSCache | None = None

    async def _discover(self) -> None:
        if self._token_endpoint:
            return
        url = f"{self._issuer}/.well-known/openid-configuration"
        async with httpx.AsyncClient() as client:
            resp = await client.get(url, timeout=10.0)
            resp.raise_for_status()
        doc = resp.json()
        self._authorization_endpoint = doc["authorization_endpoint"]
        self._token_endpoint = doc["token_endpoint"]
        self._jwks_cache = JWKSCache(doc["jwks_uri"])
        _log.info("oidc_discovered", issuer=self._issuer)

    @staticmethod
    def _pkce_pair() -> tuple[str, str]:
        verifier = base64.urlsafe_b64encode(os.urandom(32)).rstrip(b"=").decode()
        digest = hashlib.sha256(verifier.encode()).digest()
        challenge = base64.urlsafe_b64encode(digest).rstrip(b"=").decode()
        return verifier, challenge

    async def authorization_url(self, session: dict) -> str:
        """Construit l'URL d'autorisation et stocke state/nonce/verifier dans session."""
        await self._discover()
        state = base64.urlsafe_b64encode(os.urandom(16)).rstrip(b"=").decode()
        nonce = base64.urlsafe_b64encode(os.urandom(16)).rstrip(b"=").decode()
        verifier, challenge = self._pkce_pair()
        session["oidc_state"] = state
        session["oidc_nonce"] = nonce
        session["oidc_verifier"] = verifier
        params = {
            "response_type": "code",
            "client_id": self._client_id,
            "redirect_uri": self._redirect_uri,
            "scope": "openid profile email",
            "state": state,
            "nonce": nonce,
            "code_challenge": challenge,
            "code_challenge_method": "S256",
        }
        query = "&".join(f"{k}={v}" for k, v in params.items())
        return f"{self._authorization_endpoint}?{query}"

    async def exchange_and_validate(
        self, code: str, state: str, session: dict
    ) -> dict:
        """Échange le code auth contre un token, valide l'ID token, retourne les claims."""
        if session.get("oidc_state") != state:
            raise OIDCError("state mismatch")
        verifier = session.pop("oidc_verifier", "")
        nonce = session.pop("oidc_nonce", "")
        session.pop("oidc_state", None)

        await self._discover()
        async with httpx.AsyncClient() as client:
            resp = await client.post(
                self._token_endpoint,
                data={
                    "grant_type": "authorization_code",
                    "code": code,
                    "redirect_uri": self._redirect_uri,
                    "client_id": self._client_id,
                    "client_secret": self._client_secret,
                    "code_verifier": verifier,
                },
                timeout=15.0,
            )
            resp.raise_for_status()
        tokens = resp.json()
        id_token = tokens.get("id_token", "")
        if not id_token:
            raise OIDCError("no id_token in token response")
        return await self._validate_id_token(id_token, nonce)

    async def _validate_id_token(self, id_token: str, nonce: str) -> dict:
        assert self._jwks_cache is not None
        claims = None
        last_error: Exception | None = None
        for refetch in (False, True):
            keyset = await self._jwks_cache.get_keyset(refetch=refetch)
            try:
                claims = jose_jwt.decode(id_token, keyset)
                break
            except JoseError as exc:
                last_error = exc
                if refetch:
                    raise OIDCError(f"id_token validation failed after JWKS refetch: {exc}") from exc
        assert claims is not None

        # Validation des claims temporels et d'identité
        try:
            claims.validate_iss()
            claims.validate_exp(now=None, leeway=self._leeway)
        except JoseError as exc:
            raise OIDCError(f"id_token claims invalid: {exc}") from exc

        claims_dict = dict(claims)
        aud = claims_dict.get("aud")
        aud_ok = aud == self._client_id or (isinstance(aud, list) and self._client_id in aud)
        if not aud_ok:
            raise OIDCError(f"aud mismatch: expected {self._client_id!r}, got {aud!r}")
        if claims_dict.get("iss") != self._issuer:
            raise OIDCError(f"iss mismatch: expected {self._issuer!r}")
        if claims_dict.get("nonce") != nonce:
            raise OIDCError("nonce mismatch")
        return claims_dict
```

- [ ] **Step 5 : Vérifier les tests**

```bash
cd backend && uv run pytest tests/auth/test_oidc.py -v
```

Attendu : `5 passed`

Si `test_validate_raises_on_expired_token` échoue à cause du leeway : ajuster le test (offset -3600 >> leeway=30).

- [ ] **Step 6 : Lint + mypy + tous les tests**

```bash
cd backend && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/ && uv run mypy src/ && uv run pytest -q
```

- [ ] **Step 7 : Commit**

```bash
git add backend/src/portal/auth/oidc.py backend/tests/auth/test_oidc.py
git commit -m "feat(auth): OIDCClient — flow code+PKCE, validation ID token, refetch JWKS"
```

---

## Task 4 : RBAC

**Files:**
- Create: `backend/src/portal/auth/rbac.py`
- Create: `backend/tests/auth/test_rbac.py`

- [ ] **Step 1 : Écrire les tests rouges**

Créer `backend/tests/auth/test_rbac.py` :

```python
from __future__ import annotations

import pytest
from unittest.mock import MagicMock
from fastapi import HTTPException


def _make_request(session: dict) -> MagicMock:
    req = MagicMock()
    req.session = session
    return req


def test_get_current_user_returns_none_when_no_session() -> None:
    from portal.auth.rbac import get_current_user

    req = _make_request({})
    user = get_current_user(req)
    assert user is None


def test_get_current_user_returns_user_info_from_session() -> None:
    from portal.auth.rbac import get_current_user, UserInfo

    req = _make_request({"user": {"login": "alice", "roles": ["dev"]}})
    user = get_current_user(req)
    assert isinstance(user, UserInfo)
    assert user.login == "alice"
    assert "dev" in user.roles


@pytest.mark.asyncio
async def test_require_user_raises_403_when_no_session() -> None:
    from portal.auth.rbac import require_user

    req = _make_request({})
    with pytest.raises(HTTPException) as exc_info:
        await require_user(req)
    assert exc_info.value.status_code == 403


@pytest.mark.asyncio
async def test_require_user_passes_for_dev_role() -> None:
    from portal.auth.rbac import require_user, UserInfo

    req = _make_request({"user": {"login": "alice", "roles": ["dev"]}})
    user = await require_user(req)
    assert isinstance(user, UserInfo)


@pytest.mark.asyncio
async def test_require_admin_raises_403_for_dev_role() -> None:
    from portal.auth.rbac import require_admin

    req = _make_request({"user": {"login": "alice", "roles": ["dev"]}})
    with pytest.raises(HTTPException) as exc_info:
        await require_admin(req)
    assert exc_info.value.status_code == 403


@pytest.mark.asyncio
async def test_require_admin_passes_for_admin_role() -> None:
    from portal.auth.rbac import require_admin, UserInfo

    req = _make_request({"user": {"login": "bob", "roles": ["admin"]}})
    user = await require_admin(req)
    assert isinstance(user, UserInfo)


def test_extract_roles_from_nested_claim() -> None:
    from portal.auth.rbac import extract_roles

    claims = {"realm_access": {"roles": ["dev", "admin"]}}
    roles = extract_roles(claims, "realm_access.roles")
    assert roles == ["dev", "admin"]


def test_extract_roles_returns_empty_on_missing_path() -> None:
    from portal.auth.rbac import extract_roles

    claims = {}
    roles = extract_roles(claims, "realm_access.roles")
    assert roles == []


def test_validate_username_accepts_valid() -> None:
    from portal.auth.rbac import validate_username

    assert validate_username("alice") == "alice"
    assert validate_username("alice123") == "alice123"
    assert validate_username("bob-smith") == "bob-smith"


def test_validate_username_rejects_uppercase() -> None:
    from portal.auth.rbac import validate_username, UsernameError

    with pytest.raises(UsernameError):
        validate_username("Alice")


def test_validate_username_rejects_path_traversal() -> None:
    from portal.auth.rbac import validate_username, UsernameError

    with pytest.raises(UsernameError):
        validate_username("../../etc/passwd")


def test_validate_username_rejects_too_short() -> None:
    from portal.auth.rbac import validate_username, UsernameError

    with pytest.raises(UsernameError):
        validate_username("a")  # 1 char seulement
```

- [ ] **Step 2 : Vérifier que les tests échouent**

```bash
cd backend && uv run pytest tests/auth/test_rbac.py -v
```

Attendu : FAIL (ImportError)

- [ ] **Step 3 : Créer `src/portal/auth/rbac.py`**

```python
from __future__ import annotations

import re
import structlog
from dataclasses import dataclass, field
from fastapi import HTTPException, Request

from ..settings import get_settings

_log = structlog.get_logger(__name__)

# Regex username : DNS-safe, max 40 chars, pas de traversal possible
# ^[a-z0-9] : commence par alphanumérique minuscule
# [a-z0-9._-]{0,38} : milieu optionnel (autorise points pour LDAP)
# [a-z0-9]$ : se termine par alphanumérique
_USERNAME_RE = re.compile(r"^[a-z0-9][a-z0-9._-]{0,38}[a-z0-9]$")


class UsernameError(ValueError):
    """Username non conforme au format attendu."""


@dataclass
class UserInfo:
    login: str
    roles: list[str] = field(default_factory=list)
    sub: str = ""


def validate_username(username: str) -> str:
    """Valide et retourne le username, ou lève UsernameError."""
    if not _USERNAME_RE.fullmatch(username):
        raise UsernameError(
            f"username {username!r} does not match required pattern "
            r"^[a-z0-9][a-z0-9._-]{0,38}[a-z0-9]$"
        )
    return username


def extract_roles(claims: dict, role_claim_path: str) -> list[str]:
    """Extrait les rôles depuis un chemin de claim imbriqué (ex. 'realm_access.roles')."""
    parts = role_claim_path.split(".")
    value: object = claims
    for part in parts:
        if not isinstance(value, dict):
            return []
        value = value.get(part)
        if value is None:
            return []
    if isinstance(value, list):
        return [str(r) for r in value]
    return []


def get_current_user(request: Request) -> UserInfo | None:
    """Retourne l'utilisateur depuis la session, ou None si absent."""
    user_data = request.session.get("user")
    if not user_data:
        return None
    return UserInfo(
        login=user_data["login"],
        roles=user_data.get("roles", []),
        sub=user_data.get("sub", ""),
    )


async def require_user(request: Request) -> UserInfo:
    """Dépendance FastAPI : user authentifié avec rôle dev ou admin."""
    settings = get_settings()
    user = get_current_user(request)
    if user is None:
        raise HTTPException(status_code=403, detail="Not authenticated")
    allowed = {settings.oidc_user_role, settings.oidc_admin_role}
    if not set(user.roles) & allowed:
        _log.warning("rbac_denied", login=user.login, roles=user.roles)
        raise HTTPException(status_code=403, detail="Insufficient role")
    return user


async def require_admin(request: Request) -> UserInfo:
    """Dépendance FastAPI : user avec rôle admin uniquement."""
    settings = get_settings()
    user = get_current_user(request)
    if user is None:
        raise HTTPException(status_code=403, detail="Not authenticated")
    if settings.oidc_admin_role not in user.roles:
        _log.warning("rbac_admin_denied", login=user.login, roles=user.roles)
        raise HTTPException(status_code=403, detail="Admin role required")
    return user
```

- [ ] **Step 4 : Mettre à jour `src/portal/config/store.py` — regex login + save_global**

Le `_LOGIN_RE` actuel n'autorise pas les points. Le mettre à jour pour correspondre au `_USERNAME_RE` de rbac.py et ajouter `save_global()` :

Dans `store.py`, remplacer :
```python
_LOGIN_RE = re.compile(r"^[a-z0-9][a-z0-9_-]{0,61}[a-z0-9]$")
```
Par :
```python
# Aligné sur le _USERNAME_RE de rbac.py (autorise points pour LDAP, max 40 chars)
_LOGIN_RE = re.compile(r"^[a-z0-9][a-z0-9._-]{0,38}[a-z0-9]$")
```

Et ajouter après `save_user` :

```python
def save_global(cfg: GlobalConfig) -> None:
    """Écriture atomique de la config globale."""
    path = _data_root() / "config.yaml"
    parent = path.parent
    fd, tmp_path = tempfile.mkstemp(dir=parent, suffix=".tmp")
    try:
        with os.fdopen(fd, "w", encoding="utf-8") as f:
            yaml.dump(cfg.model_dump(mode="json"), f, default_flow_style=False)
        os.replace(tmp_path, path)
    except Exception:
        with contextlib.suppress(OSError):
            os.unlink(tmp_path)
        raise
```

Importer `contextlib` en haut de `store.py`.

- [ ] **Step 5 : Vérifier tous les tests**

```bash
cd backend && uv run pytest tests/auth/test_rbac.py tests/config/ -v
```

Attendu : les tests rbac + config passent tous. Si des tests config échouent à cause du changement de _LOGIN_RE (logins de test trop longs), les adapter (utiliser des logins courts).

- [ ] **Step 6 : Lint + mypy**

```bash
cd backend && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/ && uv run mypy src/
```

- [ ] **Step 7 : Commit**

```bash
git add backend/src/portal/auth/rbac.py backend/tests/auth/test_rbac.py backend/src/portal/config/store.py
git commit -m "feat(auth): RBAC — UserInfo, require_user, require_admin, extract_roles, validate_username"
```

---

## Task 5 : Auth router + provisioning user

**Files:**
- Create: `backend/src/portal/auth/router.py`
- Modify: `backend/src/portal/app.py`
- Create: `backend/tests/auth/test_router.py`

- [ ] **Step 1 : Écrire les tests rouges**

Créer `backend/tests/auth/test_router.py` :

```python
from __future__ import annotations

import pytest
import respx
import httpx
from fastapi.testclient import TestClient

from tests.auth.conftest import (
    make_id_token,
    make_jwks_response,
    ISSUER,
    CLIENT_ID,
    DISCOVERY_DOC,
)


def _make_app(tmp_data_root):
    """Crée une app de test avec settings mockés."""
    import portal.settings as settings_mod
    settings_mod._settings = None

    import os
    os.environ["PORTAL_DATA_ROOT"] = str(tmp_data_root)
    os.environ["SESSION_SECRET_KEY"] = "test-secret-key-32chars-minimum!!"
    os.environ["OIDC_ISSUER"] = ISSUER
    os.environ["OIDC_CLIENT_ID"] = CLIENT_ID
    os.environ["OIDC_CLIENT_SECRET"] = "client-secret"
    os.environ["OIDC_REDIRECT_URI"] = "https://portal.test/auth/callback"

    settings_mod._settings = None
    from portal.app import create_app
    return create_app()


@pytest.mark.asyncio
async def test_login_redirects_to_keycloak(tmp_path, monkeypatch) -> None:
    with respx.mock:
        respx.get(f"{ISSUER}/.well-known/openid-configuration").mock(
            return_value=httpx.Response(200, json=DISCOVERY_DOC)
        )
        app = _make_app(tmp_path)
        with TestClient(app, follow_redirects=False) as client:
            resp = client.get("/auth/login")

    assert resp.status_code in (302, 307)
    location = resp.headers["location"]
    assert "code_challenge=" in location
    assert f"client_id={CLIENT_ID}" in location


@pytest.mark.asyncio
async def test_callback_creates_user_dir_on_first_login(tmp_path) -> None:
    nonce = "callback-nonce"
    id_token = make_id_token(
        issuer=ISSUER,
        client_id=CLIENT_ID,
        nonce=nonce,
        username="alice",
        roles=["dev"],
    )

    with respx.mock:
        respx.get(f"{ISSUER}/.well-known/openid-configuration").mock(
            return_value=httpx.Response(200, json=DISCOVERY_DOC)
        )
        respx.get(DISCOVERY_DOC["jwks_uri"]).mock(
            return_value=httpx.Response(200, json=make_jwks_response())
        )
        respx.post(DISCOVERY_DOC["token_endpoint"]).mock(
            return_value=httpx.Response(200, json={"id_token": id_token, "access_token": "acc"})
        )

        app = _make_app(tmp_path)
        with TestClient(app, follow_redirects=False) as client:
            # 1. Login pour obtenir la session avec state/nonce/verifier
            client.get("/auth/login")
            # Récupérer le state depuis les cookies de session
            # (en pratique, le TestClient gère les cookies)
            # 2. Appeler callback avec le bon state
            # Pour ce test, on injecte directement la session via le middleware
            # en utilisant un endpoint de test ou en manipulant le cookie signé.
            # Approche alternative : tester le provisioning via la fonction directement.

    # Tester le provisioning directement (unit)
    from portal.auth.router import provision_user
    await provision_user(login="alice", sub="user-sub", data_root=tmp_path)

    user_dir = tmp_path / "users" / "alice"
    assert user_dir.is_dir()
    config_file = user_dir / "config.yaml"
    assert config_file.is_file()

    import yaml
    data = yaml.safe_load(config_file.read_text(encoding="utf-8"))
    import uuid
    assert uuid.UUID(data["secret_ns"])  # valide


@pytest.mark.asyncio
async def test_provisioning_is_idempotent(tmp_path) -> None:
    """Re-login ne régénère pas le secret_ns."""
    from portal.auth.router import provision_user

    await provision_user(login="alice", sub="sub", data_root=tmp_path)
    import yaml
    first_ns = yaml.safe_load(
        (tmp_path / "users" / "alice" / "config.yaml").read_text(encoding="utf-8")
    )["secret_ns"]

    await provision_user(login="alice", sub="sub", data_root=tmp_path)
    second_ns = yaml.safe_load(
        (tmp_path / "users" / "alice" / "config.yaml").read_text(encoding="utf-8")
    )["secret_ns"]

    assert first_ns == second_ns


@pytest.mark.asyncio
async def test_callback_rejects_invalid_username(tmp_path) -> None:
    """Un username non conforme (capital, trop court) → 403, pas de dossier créé."""
    from portal.auth.router import provision_user
    from portal.auth.rbac import UsernameError

    with pytest.raises(UsernameError):
        await provision_user(login="A", sub="sub", data_root=tmp_path)

    assert not (tmp_path / "users" / "A").exists()
```

- [ ] **Step 2 : Vérifier que les tests échouent**

```bash
cd backend && uv run pytest tests/auth/test_router.py -v
```

Attendu : FAIL (ImportError)

- [ ] **Step 3 : Créer `src/portal/auth/router.py`**

```python
from __future__ import annotations

import uuid
from pathlib import Path

import structlog
import yaml
from fastapi import APIRouter, HTTPException, Request
from fastapi.responses import RedirectResponse

from ..config.store import ensure_user_dir, safe_user_path, _data_root
from ..settings import get_settings
from .oidc import OIDCClient, OIDCError
from .rbac import UsernameError, extract_roles, validate_username

_log = structlog.get_logger(__name__)
router = APIRouter(prefix="/auth", tags=["auth"])

# Instance partagée (lazy init)
_oidc_client: OIDCClient | None = None


def _get_oidc_client() -> OIDCClient:
    global _oidc_client
    if _oidc_client is None:
        settings = get_settings()
        _oidc_client = OIDCClient(
            issuer=settings.oidc_issuer,
            client_id=settings.oidc_client_id,
            client_secret=settings.oidc_client_secret,
            redirect_uri=settings.oidc_redirect_uri,
            leeway=settings.oidc_leeway,
        )
    return _oidc_client


@router.get("/login")
async def login(request: Request) -> RedirectResponse:
    url = await _get_oidc_client().authorization_url(request.session)
    return RedirectResponse(url, status_code=302)


@router.get("/callback")
async def callback(request: Request, code: str, state: str) -> RedirectResponse:
    settings = get_settings()
    try:
        claims = await _get_oidc_client().exchange_and_validate(
            code=code, state=state, session=request.session
        )
    except OIDCError as exc:
        _log.warning("oidc_callback_error", error=str(exc))
        raise HTTPException(status_code=403, detail=str(exc)) from exc

    raw_login = claims.get(settings.oidc_username_claim, "")
    try:
        login = validate_username(raw_login)
    except UsernameError as exc:
        _log.warning("oidc_invalid_username", username=raw_login)
        raise HTTPException(status_code=403, detail=str(exc)) from exc

    roles = extract_roles(claims, settings.oidc_role_claim)
    sub = claims.get("sub", "")

    await provision_user(login=login, sub=sub, data_root=_data_root())

    request.session["user"] = {"login": login, "roles": roles, "sub": sub}
    _log.info("user_logged_in", login=login, roles=roles)
    return RedirectResponse("/", status_code=302)


@router.get("/logout")
async def logout(request: Request) -> RedirectResponse:
    login = request.session.get("user", {}).get("login", "?")
    request.session.clear()
    _log.info("user_logged_out", login=login)
    return RedirectResponse("/", status_code=302)


async def provision_user(login: str, sub: str, data_root: Path) -> None:
    """Crée le répertoire et la config initiale si absent. Idempotent."""
    validate_username(login)  # lève UsernameError si invalide → pas de dossier créé
    user_dir = data_root / "users" / login
    config_path = user_dir / "config.yaml"

    if config_path.exists():
        _log.debug("user_already_provisioned", login=login)
        return

    ensure_user_dir(login)
    initial_config = {
        "version": "1",
        "secret_ns": str(uuid.uuid4()),
        "defaults": {},
        "harpocrate": {"api_key": ""},
        "git_credentials": [],
        "workspaces": [],
    }
    import tempfile, os
    fd, tmp = tempfile.mkstemp(dir=user_dir, suffix=".tmp")
    try:
        with os.fdopen(fd, "w", encoding="utf-8") as f:
            yaml.dump(initial_config, f, default_flow_style=False)
        os.replace(tmp, str(config_path))
    except Exception:
        import contextlib
        with contextlib.suppress(OSError):
            os.unlink(tmp)
        raise

    _log.info("user_provisioned", login=login, sub=sub)
```

- [ ] **Step 4 : Mettre à jour `src/portal/app.py` pour inclure le router**

```python
from __future__ import annotations

from fastapi import FastAPI
from starlette.middleware.sessions import SessionMiddleware

from .auth.router import router as auth_router
from .settings import get_settings


def create_app() -> FastAPI:
    settings = get_settings()
    app = FastAPI(title="workspace-portal", version="0.1.0")
    app.add_middleware(
        SessionMiddleware,
        secret_key=settings.session_secret_key or "dev-only-insecure-key",
        session_cookie="portal_session",
        https_only=False,
        same_site="lax",
        max_age=86400,
    )
    app.include_router(auth_router)
    return app


app = create_app()
```

- [ ] **Step 5 : Vérifier les tests**

```bash
cd backend && uv run pytest tests/auth/test_router.py -v
```

Attendu : `4 passed`

- [ ] **Step 6 : Lint + mypy + tous les tests**

```bash
cd backend && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/ && uv run mypy src/ && uv run pytest -q
```

- [ ] **Step 7 : Commit**

```bash
git add backend/src/portal/auth/router.py backend/src/portal/app.py backend/tests/auth/test_router.py
git commit -m "feat(auth): router OIDC /login /callback /logout + provisioning user idempotent"
```

---

## Task 6 : Endpoints /me/*

**Files:**
- Create: `backend/src/portal/routes/__init__.py`
- Create: `backend/src/portal/routes/me.py`
- Modify: `backend/src/portal/app.py`
- Create: `backend/tests/routes/__init__.py`
- Create: `backend/tests/routes/test_me.py`

- [ ] **Step 1 : Écrire les tests rouges**

Créer `backend/tests/routes/test_me.py` :

```python
from __future__ import annotations

import pytest
import yaml
from fastapi.testclient import TestClient


def _make_test_app(tmp_data_root, role: str = "dev"):
    """Crée une app avec user 'alice' pré-authentifié en session."""
    import portal.settings as settings_mod
    import os
    settings_mod._settings = None
    os.environ["PORTAL_DATA_ROOT"] = str(tmp_data_root)
    os.environ["SESSION_SECRET_KEY"] = "test-secret-key-32chars-minimum!!"
    os.environ["OIDC_ISSUER"] = "https://kc.test/realms/yoops"
    os.environ["OIDC_CLIENT_ID"] = "portal"
    settings_mod._settings = None

    from portal.app import create_app
    from portal.auth.rbac import UserInfo, require_user, require_admin

    app = create_app()

    user = UserInfo(login="alice", roles=[role])
    app.dependency_overrides[require_user] = lambda: user
    if role == "admin":
        app.dependency_overrides[require_admin] = lambda: user
    return app


def _provision_alice(tmp_data_root):
    """Crée la config de alice dans tmp_data_root."""
    import asyncio
    from portal.auth.router import provision_user
    asyncio.get_event_loop().run_until_complete(
        provision_user(login="alice", sub="sub", data_root=tmp_data_root)
    )


def test_get_me_config_returns_user_config(tmp_path) -> None:
    _provision_alice(tmp_path)
    import os
    os.environ["PORTAL_DATA_ROOT"] = str(tmp_path)
    app = _make_test_app(tmp_path)
    with TestClient(app) as client:
        resp = client.get("/me/config")
    assert resp.status_code == 200
    data = resp.json()
    assert "secret_ns" in data


def test_put_me_config_updates_defaults(tmp_path) -> None:
    _provision_alice(tmp_path)
    import os
    os.environ["PORTAL_DATA_ROOT"] = str(tmp_path)
    app = _make_test_app(tmp_path)
    with TestClient(app) as client:
        resp = client.put("/me/config", json={"defaults": {"ide": "openvscode", "idle_timeout": "2h"}})
    assert resp.status_code == 200


def test_put_me_config_rejects_unknown_field(tmp_path) -> None:
    _provision_alice(tmp_path)
    app = _make_test_app(tmp_path)
    with TestClient(app) as client:
        resp = client.put("/me/config", json={"unknown_field": "value"})
    assert resp.status_code == 422


def test_get_me_workspaces_returns_empty_list(tmp_path) -> None:
    _provision_alice(tmp_path)
    app = _make_test_app(tmp_path)
    with TestClient(app) as client:
        resp = client.get("/me/workspaces")
    assert resp.status_code == 200
    assert resp.json() == []


def test_post_me_workspace_adds_workspace(tmp_path) -> None:
    _provision_alice(tmp_path)
    app = _make_test_app(tmp_path)
    ws = {
        "name": "myapp",
        "source": "git@github.com:user/repo.git",
        "branch": "main",
        "host": None,
        "git_credential": None,
        "template": None,
        "devcontainer_path": None,
        "recipes": [],
        "ide": None,
        "idle_timeout": None,
        "env": {},
        "expose": None,
    }
    with TestClient(app) as client:
        resp = client.post("/me/workspaces", json=ws)
    assert resp.status_code in (200, 201)
    with TestClient(app) as client:
        resp = client.get("/me/workspaces")
    names = [w["name"] for w in resp.json()]
    assert "myapp" in names


def test_delete_me_workspace_removes_workspace(tmp_path) -> None:
    _provision_alice(tmp_path)
    app = _make_test_app(tmp_path)
    ws = {"name": "todelete", "source": "git@github.com:user/repo.git", "branch": "main",
          "host": None, "git_credential": None, "template": None, "devcontainer_path": None,
          "recipes": [], "ide": None, "idle_timeout": None, "env": {}, "expose": None}
    with TestClient(app) as client:
        client.post("/me/workspaces", json=ws)
        resp = client.delete("/me/workspaces/todelete")
    assert resp.status_code == 200
    with TestClient(app) as client:
        resp = client.get("/me/workspaces")
    names = [w["name"] for w in resp.json()]
    assert "todelete" not in names


def test_require_user_blocks_unauthenticated(tmp_path) -> None:
    """Sans dependency_overrides, /me/config retourne 403."""
    _provision_alice(tmp_path)
    import portal.settings as settings_mod
    settings_mod._settings = None
    import os
    os.environ["PORTAL_DATA_ROOT"] = str(tmp_path)
    from portal.app import create_app
    app = create_app()  # sans override
    with TestClient(app) as client:
        resp = client.get("/me/config")
    assert resp.status_code == 403
```

- [ ] **Step 2 : Exécuter les tests pour vérifier qu'ils échouent**

```bash
cd backend && uv run pytest tests/routes/test_me.py -v
```

Attendu : FAIL (ImportError)

- [ ] **Step 3 : Créer `src/portal/routes/__init__.py`** (vide)

- [ ] **Step 4 : Créer `src/portal/routes/me.py`**

```python
from __future__ import annotations

import structlog
from fastapi import APIRouter, Depends, HTTPException

from ..auth.rbac import UserInfo, require_user
from ..config.models import UserConfig, WorkspaceSpec
from ..config.store import load_user, save_user

_log = structlog.get_logger(__name__)
router = APIRouter(tags=["me"])


@router.get("/config")
async def get_config(user: UserInfo = Depends(require_user)) -> dict:
    cfg = load_user(user.login)
    return cfg.model_dump(mode="json")


@router.put("/config")
async def put_config(
    updates: dict, user: UserInfo = Depends(require_user)
) -> dict:
    cfg = load_user(user.login)
    merged = cfg.model_dump(mode="json")
    merged.update(updates)
    try:
        new_cfg = UserConfig.model_validate(merged)
    except Exception as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc
    save_user(user.login, new_cfg)
    _log.info("user_config_updated", login=user.login)
    return new_cfg.model_dump(mode="json")


@router.get("/workspaces")
async def list_workspaces(user: UserInfo = Depends(require_user)) -> list:
    cfg = load_user(user.login)
    return [ws.model_dump(mode="json") for ws in cfg.workspaces]


@router.post("/workspaces", status_code=201)
async def add_workspace(
    workspace: WorkspaceSpec, user: UserInfo = Depends(require_user)
) -> dict:
    cfg = load_user(user.login)
    if any(ws.name == workspace.name for ws in cfg.workspaces):
        raise HTTPException(status_code=409, detail=f"Workspace {workspace.name!r} already exists")
    cfg.workspaces.append(workspace)
    save_user(user.login, cfg)
    _log.info("workspace_added", login=user.login, name=workspace.name)
    return workspace.model_dump(mode="json")


@router.delete("/workspaces/{name}")
async def delete_workspace(name: str, user: UserInfo = Depends(require_user)) -> dict:
    cfg = load_user(user.login)
    before = len(cfg.workspaces)
    cfg.workspaces = [ws for ws in cfg.workspaces if ws.name != name]
    if len(cfg.workspaces) == before:
        raise HTTPException(status_code=404, detail=f"Workspace {name!r} not found")
    save_user(user.login, cfg)
    _log.info("workspace_deleted", login=user.login, name=name)
    return {"deleted": name}
```

- [ ] **Step 5 : Mettre à jour `app.py` pour inclure le router me**

```python
from __future__ import annotations

from fastapi import FastAPI
from starlette.middleware.sessions import SessionMiddleware

from .auth.router import router as auth_router
from .routes.me import router as me_router
from .settings import get_settings


def create_app() -> FastAPI:
    settings = get_settings()
    app = FastAPI(title="workspace-portal", version="0.1.0")
    app.add_middleware(
        SessionMiddleware,
        secret_key=settings.session_secret_key or "dev-only-insecure-key",
        session_cookie="portal_session",
        https_only=False,
        same_site="lax",
        max_age=86400,
    )
    app.include_router(auth_router)
    app.include_router(me_router, prefix="/me")
    return app


app = create_app()
```

- [ ] **Step 6 : Adapter les modèles si nécessaire**

`WorkspaceSpec` dans models.py doit avoir tous les champs optionnels utilisés dans les tests. Vérifier que `source`, `branch`, `host`, `git_credential`, `template`, `devcontainer_path`, `expose` sont présents avec defaults. Si WorkspaceSpec n'a que `name`, `recipes`, `env` (minimal M1), l'étendre :

```python
class ExposeConfig(BaseModel):
    model_config = ConfigDict(extra="forbid")
    hostname: str = ""


class WorkspaceSpec(BaseModel):
    model_config = ConfigDict(extra="forbid")
    name: str
    source: str = ""
    branch: str = "main"
    git_credential: str | None = None
    host: str | None = None
    template: str | None = None
    devcontainer_path: str | None = None
    recipes: list[str] = Field(default_factory=list)
    ide: str | None = None
    idle_timeout: str | None = None
    env: dict[str, str] = Field(default_factory=dict)
    expose: ExposeConfig | None = None

    @field_validator("name")
    @classmethod
    def validate_name(cls, v: str) -> str:
        if not _WORKSPACE_NAME_RE.fullmatch(v):
            raise ValueError(f"name {v!r} must match ^[a-z0-9][a-z0-9-]{{0,30}}[a-z0-9]$")
        return v
```

- [ ] **Step 7 : Vérifier les tests**

```bash
cd backend && uv run pytest tests/routes/test_me.py -v
```

Attendu : `7 passed`

- [ ] **Step 8 : Lint + mypy + tous les tests**

```bash
cd backend && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/ && uv run mypy src/ && uv run pytest -q
```

- [ ] **Step 9 : Commit**

```bash
git add backend/src/portal/routes/ backend/tests/routes/test_me.py backend/src/portal/app.py backend/src/portal/config/models.py
git commit -m "feat(routes): endpoints /me/config et /me/workspaces (CRUD)"
```

---

## Task 7 : Endpoints /admin/*

**Files:**
- Create: `backend/src/portal/routes/admin.py`
- Modify: `backend/src/portal/app.py`
- Create: `backend/tests/routes/test_admin.py`

- [ ] **Step 1 : Écrire les tests rouges**

Créer `backend/tests/routes/test_admin.py` :

```python
from __future__ import annotations

import pytest
import yaml
from pathlib import Path
from fastapi.testclient import TestClient


def _make_global_config(tmp_data_root: Path) -> None:
    """Crée un config.yaml global minimal."""
    config = {
        "version": "1",
        "server": {"listen": "0.0.0.0:8080", "base_domain": "dev.yoops.org",
                   "external_url": "https://dev.yoops.org", "dev_mode": False,
                   "log": {"level": "info", "format": "text", "output": ""}},
        "auth": {"oidc": {"issuer": "https://kc.test", "client_id": "portal",
                           "client_secret": "", "scopes": ["openid"],
                           "role_claim": "realm_access.roles", "admin_role": "admin",
                           "user_role": "dev", "username_claim": "preferred_username"}},
        "secrets": {"backend": "inline", "harpocrate": {"url": "", "api_key": "", "base_path": "devpod"}},
        "devpod": {"binary": "/usr/local/bin/devpod", "defaults": {"ide": "openvscode",
                    "idle_timeout": "2h", "dotfiles": ""}, "client_cert_path": "/data/certs/portal"},
        "hosts": [{"name": "local", "default": True, "type": "docker-tls",
                   "docker_host": "tcp://192.168.1.50:2376"}],
        "caddy": {"admin_api": "http://caddy:2019"},
        "cloudflare_manager": {"url": "http://cfm:8000", "api_key": ""},
    }
    (tmp_data_root / "config.yaml").write_text(
        yaml.dump(config, default_flow_style=False), encoding="utf-8"
    )


def _make_admin_app(tmp_data_root: Path):
    import portal.settings as settings_mod
    import os
    settings_mod._settings = None
    os.environ["PORTAL_DATA_ROOT"] = str(tmp_data_root)
    os.environ["SESSION_SECRET_KEY"] = "test-secret-key-32chars-minimum!!"
    settings_mod._settings = None
    from portal.app import create_app
    from portal.auth.rbac import UserInfo, require_admin
    app = create_app()
    app.dependency_overrides[require_admin] = lambda: UserInfo(login="bob", roles=["admin"])
    return app


def _make_dev_app(tmp_data_root: Path):
    import portal.settings as settings_mod
    import os
    settings_mod._settings = None
    os.environ["PORTAL_DATA_ROOT"] = str(tmp_data_root)
    os.environ["SESSION_SECRET_KEY"] = "test-secret-key-32chars-minimum!!"
    settings_mod._settings = None
    from portal.app import create_app
    # Pas d'override require_admin → 403
    return create_app()


def test_get_admin_config_returns_global_config(tmp_path) -> None:
    _make_global_config(tmp_path)
    app = _make_admin_app(tmp_path)
    with TestClient(app) as client:
        resp = client.get("/admin/config")
    assert resp.status_code == 200
    data = resp.json()
    assert data["version"] == "1"
    assert "hosts" in data


def test_get_admin_config_requires_admin(tmp_path) -> None:
    _make_global_config(tmp_path)
    app = _make_dev_app(tmp_path)
    with TestClient(app) as client:
        resp = client.get("/admin/config")
    assert resp.status_code == 403


def test_get_admin_hosts_returns_host_list(tmp_path) -> None:
    _make_global_config(tmp_path)
    app = _make_admin_app(tmp_path)
    with TestClient(app) as client:
        resp = client.get("/admin/hosts")
    assert resp.status_code == 200
    hosts = resp.json()
    assert any(h["name"] == "local" for h in hosts)


def test_post_admin_hosts_adds_host(tmp_path) -> None:
    _make_global_config(tmp_path)
    app = _make_admin_app(tmp_path)
    new_host = {
        "name": "pve1",
        "default": False,
        "type": "ssh",
        "address": "devops@192.168.1.40",
        "key_path": "/data/keys/hosts/pve1_ed25519",
    }
    with TestClient(app) as client:
        resp = client.post("/admin/hosts", json=new_host)
    assert resp.status_code in (200, 201)
    with TestClient(app) as client:
        resp = client.get("/admin/hosts")
    names = [h["name"] for h in resp.json()]
    assert "pve1" in names
```

- [ ] **Step 2 : Vérifier que les tests échouent**

```bash
cd backend && uv run pytest tests/routes/test_admin.py -v
```

Attendu : FAIL

- [ ] **Step 3 : Créer `src/portal/routes/admin.py`**

```python
from __future__ import annotations

import structlog
from fastapi import APIRouter, Depends, HTTPException

from ..auth.rbac import UserInfo, require_admin
from ..config.models import GlobalConfig, HostSpec
from ..config.store import load_global, save_global

_log = structlog.get_logger(__name__)
router = APIRouter(tags=["admin"])


@router.get("/config")
async def get_admin_config(user: UserInfo = Depends(require_admin)) -> dict:
    cfg = load_global()
    return cfg.model_dump(mode="json")


@router.put("/config")
async def put_admin_config(
    updates: dict, user: UserInfo = Depends(require_admin)
) -> dict:
    cfg = load_global()
    merged = cfg.model_dump(mode="json")
    merged.update(updates)
    try:
        new_cfg = GlobalConfig.model_validate(merged)
    except Exception as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc
    save_global(new_cfg)
    _log.info("global_config_updated", by=user.login)
    return new_cfg.model_dump(mode="json")


@router.get("/hosts")
async def list_hosts(user: UserInfo = Depends(require_admin)) -> list:
    cfg = load_global()
    return [h.model_dump(mode="json") for h in cfg.hosts]


@router.post("/hosts", status_code=201)
async def add_host(
    host: HostSpec, user: UserInfo = Depends(require_admin)
) -> dict:
    cfg = load_global()
    if any(h.name == host.name for h in cfg.hosts):
        raise HTTPException(status_code=409, detail=f"Host {host.name!r} already exists")
    cfg.hosts.append(host)
    save_global(cfg)
    _log.info("host_added", name=host.name, by=user.login)
    return host.model_dump(mode="json")
```

- [ ] **Step 4 : S'assurer que `HostSpec` est défini dans `models.py`**

Vérifier que `GlobalConfig.hosts` est une liste de `HostSpec`. Si `HostSpec` n'existe pas encore (M1 avait peut-être une définition minimale), l'ajouter :

```python
class HostSpec(BaseModel):
    model_config = ConfigDict(extra="forbid")
    name: str
    default: bool = False
    type: Literal["docker-tls", "ssh"] = "docker-tls"
    docker_host: str = ""
    address: str = ""   # pour SSH
    key_path: str = ""  # pour SSH
```

Vérifier que `GlobalConfig` a bien `hosts: list[HostSpec] = Field(default_factory=list)`.

- [ ] **Step 5 : Ajouter admin router dans `app.py`**

```python
from .routes.admin import router as admin_router
# ...
app.include_router(admin_router, prefix="/admin")
```

- [ ] **Step 6 : Vérifier les tests**

```bash
cd backend && uv run pytest tests/routes/test_admin.py -v
```

Attendu : `4 passed`

- [ ] **Step 7 : Lint + mypy + tous les tests**

```bash
cd backend && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/ && uv run mypy src/ && uv run pytest -q
```

Attendu : 0 erreurs, tous les tests verts.

- [ ] **Step 8 : Commit**

```bash
git add backend/src/portal/routes/admin.py backend/tests/routes/test_admin.py backend/src/portal/app.py backend/src/portal/config/models.py
git commit -m "feat(routes): endpoints /admin/config et /admin/hosts (require_admin)"
```

---

## Definition of Done M2

Avant de déclarer M2 terminé, vérifier **tous** ces points :

- [ ] `uv run pytest tests/ -v` → 0 échec
- [ ] `uv run ruff check src/ tests/` → 0 erreur
- [ ] `uv run ruff format --check src/ tests/` → 0 fichier non conforme
- [ ] `uv run mypy src/` → 0 erreur
- [ ] Tous les pièges M2 couverts :
  - §C-16 : ID token validé (iss, aud, exp, nonce, signature JWKS)
  - §C-17 : rôles extraits via chemin de claim configurable
  - §C-18 : username validé avant usage comme nom de dossier
  - §C-19 : session cookie HttpOnly + SameSite=Lax
- [ ] Provisioning idempotent (pas de régénération secret_ns)
- [ ] `require_admin` retourne 403 pour un user avec rôle `dev`
- [ ] Aucun secret en clair dans le code (client_secret lu depuis env)
- [ ] Tous les commits sur la branche `dev`
