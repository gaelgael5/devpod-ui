id: searxng
name: SearXNG
description: Moteur de recherche méta privé — backend de recherche web pour agents MCP
version: 2024.11.4
tags:
  - search
  - web
  - privacy
  - mcp
image: searxng/searxng:2024.11.4
parameters:
  - key: SECRET_KEY
    label: "Clé secrète"
    description: "Clé secrète Flask (générer avec: python3 -c \"import secrets; print(secrets.token_hex(32))\")"
    type: secret
    required: true
    secret_ref_hint: "searxng/secret-key"
