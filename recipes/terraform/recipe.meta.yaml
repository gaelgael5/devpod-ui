id: terraform
version: 1.0.0
description: "Terraform via dépôt officiel HashiCorp apt"
options:
  version:
    type: string
    default: "latest"
    description: "Version à épingler (ex : 1.9.0) ou 'latest' pour la dernière stable"
