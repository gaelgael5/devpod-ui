id: git
key: b744a99d-d483-4faf-aa77-c263f48f896b
version: 1.0.0
description: "Configure Git globalement (user.name / user.email via variables d'environnement)"
options:
  user_name:
    type: string
    default: "dev"
    description: "Valeur de git config user.name"
  user_email:
    type: string
    default: "dev@example.com"
    description: "Valeur de git config user.email"
