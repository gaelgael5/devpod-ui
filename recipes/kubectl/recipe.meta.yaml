id: kubectl
version: 1.0.0
description: "kubectl + Helm — pilotage de clusters Kubernetes"
options:
  kubectl_version:
    type: string
    default: "1.30"
    description: "Version majeure.mineure de kubectl (1.29 | 1.30 | 1.31)"
