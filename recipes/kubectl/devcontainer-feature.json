{
  "id": "kubectl",
  "version": "1.0.0",
  "name": "kubectl + Helm",
  "description": "kubectl + Helm — pilotage de clusters Kubernetes",
  "options": {
    "kubectl_version": {
      "type": "string",
      "default": "1.30",
      "description": "Version majeure.mineure de kubectl",
      "proposals": ["1.29", "1.30", "1.31"]
    }
  }
}
