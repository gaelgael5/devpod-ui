{
    # API admin accessible par le container portal (réseau Docker internal)
    admin 0.0.0.0:2019
}

# Dev : catch-all port 80 → portail (pas de TLS, pas de restriction de domaine)
# Les routes workspace sont ajoutées dynamiquement via l'API admin Caddy.
:80 {
    reverse_proxy portal:8080
}
