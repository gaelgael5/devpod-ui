apiVersion: 1

datasources:
  - name: Loki
    type: loki
    # uid stable : requis par les règles d'alerte provisionnées (alerting/*.yaml)
    uid: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
    jsonData:
      maxLines: 1000
